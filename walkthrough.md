# Walkthrough

## 2026-08-31 — 반복 탭 감사 도구 오탐 정리

- 고유 단서·순서 진행을 이미 강제하는 구석기 6단계와 적 함선 표적을 요구하는 명량 포격 단계를 단순 반복 탭 후보에서 제외했다.
- JSON 핫스팟이 없는 레거시 렌더러의 실제 런타임 계약을 감사 도구에 명시해, 구조적 후보 수를 실제 위험에 맞게 집계하도록 했다.
- 앱 런타임 동작은 변경하지 않았으며, 감사 결과와 전체 검증을 다시 실행했다.

## 2026-08-31 — 청동기 거석 운반 반복 탭 내성 보강

- `regular_bronze_age` 3단계의 반복 슬라이더 활동을 흙 경사로·통나무 굴림대·공동 노동 핫스팟 활동으로 전환했다.
- `dolmen-step3`에서도 핫스팟 입력이 우선 처리되도록 보완하고, 캔버스·대체 버튼의 고유 단서 기록을 동기화했다.
- 청동기 거석 운반 장면을 추가해 세 단서의 위치와 역사 맥락을 화면에 연결했다.
- 820×1180 Chrome에서 동일 단서 반복 방지, 세 단서 완료 후 선택지 활성화, 콘솔 오류 0건을 확인했다.
- 구조적 반복 탭 후보는 51개에서 50개로 감소했다.

## 2026-08-31 — 브라우저 회귀 점검 복구 및 대표 수직 슬라이스 완료

- 브라우저 플러그인 갱신 후 820×1180 세로형 태블릿 뷰포트에서 실제 화면 점검을 수행했다.
- 포털 초기 화면과 2단원 7차시·3단원 12차시의 primary MUD 연결이 의도대로 동작하는지 확인했다.
- `regular_paleolithic`에서 필수 단서 잠금, 순서 오류 피드백, 오답 IF 분기, 정상 단계 이동을 확인했다.
- 성찰 단서 3개를 모두 확인하기 전 완료 버튼이 잠긴 상태를 확인하고, 완료 후 성찰 UI·도감 언급·콘솔 오류 0건을 확인했다.
- 상세 결과는 `BROWSER_REGRESSION_CHECKLIST.md`에 기록했다.

## 2026-08-31 — P1-02 1차 반영분 브라우저 확인

- 820×1180 세로형 태블릿 뷰포트에서 `regular_independence`, `regular_modern_open`, `regular_post_war`, `regular_japanese_rule_1`의 첫 단계를 확인했다.
- 네 MUD 모두 자료 단서용 대체 버튼이 표시되고, 단서 확인 전 선택지가 잠기며, 콘솔 오류가 없었다.
- 1~3단계 전체 흐름과 실제 학생 소요 시간 측정은 후속 검증으로 남겼다.

## 2026-08-31 — 시뮬레이터 상태 접근성 보강

- 캔버스가 안내문·피드백 영역을 `aria-describedby`로 참조하도록 연결했다.
- 시뮬레이터 안내문을 보조기기용 `note`로 지정하고 대체 조작 영역을 `group`으로 명시했다.
- 필수 활동 전후 선택지의 `aria-disabled` 상태를 실제 버튼 잠금 상태와 동기화했다.
- 검증: 전체 데이터·MUD·계약·런타임 검사, JS 문법 검사, `git diff --check` 통과.

## 2026-08-31 — 선택지 식별 표시 개선

- MUD 선택지 오른쪽의 반복되는 `선택 n →` 배지를 A/B/C 식별자로 변경했다.
- 정답 여부는 노출하지 않으면서 선택지별 색상과 모서리 형태를 다르게 적용해 모바일 화면에서도 버튼을 구분하기 쉽게 했다.
- 기존 선택지 섞기, 잠금 상태, 접근성용 `aria-label`은 유지했다.
- 검증: `node --check js/mudEngine.js`, `git diff --check` 통과.

## 2026-08-31 — Regular 반복 탭 내성 1차 묶음

- `regular_independence` 1~3단계의 단순 게이지 활동을 `hotspot-discovery`로 전환했다.
- 3·1 운동의 독립 의지·여러 계층 참여·비폭력 시위, 아우내 장터의 학생·주민 참여, 임시정부의 임시헌장·국민 주권·민주공화제 단서를 각각 선언했다.
- `independence-evidence` 장면과 공통 대체 버튼을 연결해 캔버스와 버튼이 같은 단서 진행 상태를 사용하도록 했다.
- 구조적 반복 탭 후보는 66개에서 63개로 감소했다. 이 감사는 실제 학생 소요 시간 측정이 아닌 구조 선별이다.
- 검증: 게임 데이터 JSON, MUD 무결성, MUD 계약, 시뮬레이터 런타임, JavaScript 문법, 반복 탭 감사, `git diff --check` 통과.

## 2026-08-31 — Regular 반복 탭 내성 2차 묶음

- `regular_modern_open` 1~3단계의 단순 게이지 활동을 `hotspot-discovery`로 전환했다.
- 강화도 조약의 무력 위협·치외법권·해안 측량권, 근대 시설의 병원·우편·이용 한계, 전차·전등·도시 생활 변화 단서를 선언했다.
- 기존 4단계의 `modern-open-evidence` 장면과 공통 대체 버튼을 재사용했다.
- 구조적 반복 탭 후보는 63개에서 60개로 감소했다. 실제 학생 시간 측정은 별도 검토 대상이다.
- 검증: 게임 데이터 JSON, MUD 무결성, MUD 계약, 시뮬레이터 런타임, JavaScript 문법, 반복 탭 감사, `git diff --check` 통과.

## 2026-08-31 — Regular 반복 탭 내성 3차 묶음

- `regular_post_war` 1~3단계의 단순 게이지 활동을 `hotspot-discovery`로 전환했다.
- 전쟁 피해·서로 돕기·재건 조건, 천막 교실·배움 이어가기·지역별 조건, 이산가족 기록·DMZ·대화·교류 단서를 선언했다.
- 기존 4단계의 `post-war-evidence` 장면과 공통 대체 버튼을 재사용했다.
- 구조적 반복 탭 후보는 60개에서 57개로 감소했다. 실제 학생 시간 측정은 별도 검토 대상이다.
- 검증: 게임 데이터 JSON, MUD 무결성, MUD 계약, 시뮬레이터 런타임, JavaScript 문법, 반복 탭 감사, `git diff --check` 통과.

## 2026-08-31 — Regular 반복 탭 내성 4차 묶음

- `regular_japanese_rule_1` 1~3단계의 단순 게이지 활동을 `hotspot-discovery`로 전환했다.
- 헌병 경찰의 통치·즉결 처벌·일상 통제, 토지 조사·소유권 변화·농민 부담, 비밀 결사·서약·저항의 위험 단서를 선언했다.
- 기존 4단계의 `colonial-1910s-evidence` 장면과 공통 대체 버튼을 재사용했다.
- 민감한 역사 주제의 기존 서술을 대량 수정하지 않고, 자료 기반 관찰 단서만 추가했다.
- 구조적 반복 탭 후보는 57개에서 54개로 감소했다. 실제 학생 시간 측정은 별도 검토 대상이다.
- 검증: 게임 데이터 JSON, MUD 무결성, MUD 계약, 시뮬레이터 런타임, JavaScript 문법, 반복 탭 감사, `git diff --check` 통과.

## 2026-08-31 — 민감한 역사 주제 자료 중심 상호작용

- `regular_japanese_rule_2` 1~3단계의 단순 게이지 활동을 `hotspot-discovery`로 전환했다.
- 황국신민화 정책은 정책·이름·학교생활 자료, 강제 동원 단계는 동원 기록·피해자 증언·기억 활동 자료, 조선어학회 단계는 사전 원고·맞춤법·언어생활 자료를 확인하도록 구성했다.
- 피해 상황의 직접 연기·재현이나 공포·죄책감 기반의 정답 유도를 추가하지 않았다.
- 기존 4단계의 `colonial-1930s-evidence` 장면과 공통 대체 버튼을 재사용했다.
- 구조적 반복 탭 후보는 54개에서 51개로 감소했다. 실제 학생 시간과 교사 관점의 최종 검토는 별도 대기 항목이다.
- 검증: 게임 데이터 JSON, MUD 무결성, MUD 계약, 시뮬레이터 런타임, JavaScript 문법, 반복 탭 감사, `git diff --check` 통과.

## 2026-08-31 — 누적 변경 후 감사 기준 갱신

- 5개 Regular MUD의 상호작용 변경을 반영해 활동 시간 감사 결과를 다시 생성했다.
- Regular 28종 중 보완 우선 대상은 16종, 기준 충족 추정은 12종으로 집계되었다. 이 수치는 실제 학생 측정이 아닌 설계 지표다.
- IF 스테이지 검토 신호는 94개, 유물 카드 표현 검토 신호는 6개로 확인되었다.
- 검증: `scripts/07_audit_activity_duration.py`, `scripts/09_validate_mud_sources.py`, `scripts/10_audit_if_stages.py`, `scripts/11_audit_artifacts.py` 실행.

## 2026-08-31 — IF 스테이지 품질 1차 대상 선별

- IF 스테이지 94개 검토 신호를 설명 길이와 자료·비교 단서 수로 재선별했다.
- `regular_korean_war` 3-1, `regular_joseon_diplomacy` 1-1·2-1·3-1, `regular_independence_army` 3-1, `regular_goryeo_culture` 1-1을 1차 검토 대상으로 정했다.
- 문장 보강 전 교육적 기준과 변경 범위를 [`implementation_plan_if_stage_quality_batch.md`](./docs/plans/implementation_plan_if_stage_quality_batch.md)에 기록했다.
- 이번 단계에서는 코드와 콘텐츠를 수정하지 않았다.

## 2026-08-31 — IF 스테이지 품질 1차 문장 보강

- `regular_korean_war` 3-1, `regular_joseon_diplomacy` 1-1·2-1·3-1, `regular_independence_army` 3-1, `regular_goryeo_culture` 1-1의 narrative를 자료·기록·비교 단서와 연결했다.
- 고려 문화 IF 단계의 부적절한 복귀 선택지를 목재 가공·보관 환경 자료 비교 행동으로 교체했다.
- 가상 결과와 확인 가능한 역사 사실을 구분하고, 공포나 죄책감으로 정답을 유도하지 않는 기준을 유지했다.
- IF 감사 신호는 94개에서 92개로 감소했다. 남은 신호는 교사 관점의 문장 난이도와 최소 길이 기준을 추가 검토한다.
- 검증: 게임 데이터 JSON, MUD 무결성, MUD 계약, 출처 메타데이터, IF 감사, 시뮬레이터 런타임, JavaScript 문법, `git diff --check` 통과.

## 2026-08-31 — 접근성·상태 표현 1차

- 시뮬레이터 캔버스에 `role="img"`와 대체 조작 안내를 추가했다.
- 게이지 진행률에 `role="status"`와 `aria-live`를 추가했다.
- 퀴즈·도감 모달에 `role="dialog"`, `aria-modal`, 제목 연결을 추가했다.
- 모달 닫기 버튼에 목적을 설명하는 `aria-label`을 추가했다.
- 실제 키보드 포커스·보조기기 점검은 브라우저 확인 대기 항목으로 남겼다.

## 2026-08-31 — 유물·보상 설명 표현 1차

- `art_1`, `art_3`, `art_4`, `art_15`, `art_16`, `art_18`의 과장·단정 표현을 자료의 의미와 역사적 범위에 맞게 완화했다.
- 유물명·시대·등급·보상 ID 연결은 변경하지 않았다.
- 자동 유물 감사 신호는 6종에서 0종으로 감소했다. 교사 관점의 최종 사실·문장 난이도 검토는 남겨 두었다.
- 검증: 게임 데이터 JSON, MUD 무결성, 출처 메타데이터, 유물 감사, 시뮬레이터 런타임, JavaScript 문법, `git diff --check` 통과.

## 2026-08-31 — 설계 정합성 대표 수직 슬라이스 1차 반영

- `WIREFRAMES.md`에서 실제 화면에 없는 `[자료 보기]` 요소를 제거했다.
- `regular_paleolithic` 6단계 성찰 시뮬레이터를 `required: true`로 변경해 성찰 단서 확인 후 엔딩으로 진행하도록 설계 기준을 반영했다.
- 구현 계획은 정적 검증 완료·브라우저 시각 점검 대기 상태로 유지한다. 브라우저 연결 플러그인 버전 오류로 태블릿 화면 검증은 수행하지 못했다.
- 검증: 게임 데이터 JSON, MUD 무결성, MUD 계약, 시뮬레이터 런타임, `mudEngine.js`·`mudSimulators.js` 문법, `git diff --check` 통과.

## 2026-08-28 — P1 Regular 시간·IF 구조 감사

- Regular MUD 28종의 활동 시간 구조를 다시 생성해, 10분 이내·설계 목표 약 9분 기준과 반복 탭 조기 종료 위험을 분리해 확인했다.
- 반복 탭 구조 감사는 65개 후보를 확인했으며, 장면이 있는 단계는 Claude 소유 범위이므로 좌표·핫스팟·상호작용·완료조건을 변경하지 않았다.
- Regular 재시도(IF) 단계 94개를 `docs/audits/if_stage_audit.md`로 선별했다. 설명 길이, 자료·비교 단서, 원 단계 복귀 선택지를 기준으로 검토 신호를 기록했으며, 문장 보강은 교사 검토 후 진행할 미완료 항목으로 남겼다.
- 검증: `scripts/07_audit_activity_duration.py`, `scripts/09_audit_tap_resistance.py`, `scripts/10_audit_if_stages.py` 실행 완료. 이번 단계에서는 JSON 장면 필드를 수정하지 않았다.

## 2026-08-28 — P2 MUD 등록 차시 기준 정합화

- 커리큘럼 화면의 `lessonDisplay`가 단원 내부 번호를 사용한다는 점을 확인하고, 전역 번호로 기록된 세 Regular 항목을 화면 표시 기준으로 정렬했다.
- `app.js`의 인덱스 우선 조회가 세종·3·1 운동·6·25 MUD를 해당 화면 차시에 직접 연결하도록 했고, 비등록 차시용 fallback은 유지했다.
- 장기 기준을 `DECISIONS.md`의 D-008에 기록했다. Claude 담당 시뮬레이터 장면 필드는 변경하지 않았다.

## 2026-08-28 — P2 유물·보상 설명 1차 사실 범위 검수

- MUD 보상 35개 연결을 전수 확인해 누락 ID나 보상명 불일치가 없음을 확인했다.
- `art_9`, `art_10`, `art_13`의 시대 표기를 각각 고려 시대, 고려 시대, 조선 후기로 바로잡았다.
- 직지 설명의 단정적 표현을 `현존하는 세계에서 가장 오래된 금속활자본으로 알려진`으로 정밀화했다. UNESCO와 국가유산포털의 현존·기록 범위를 반영한 표현이다.
- 36종 전체의 출처·문장 난이도 검수는 아직 완료하지 않았으며 BACKLOG에 남겼다.

## 2026-08-28 — P2 유물·보상 카드 구조 감사

- 유물 36종의 필수 필드, 과장·단정 표현, 설명 길이를 자동 선별하는 `scripts/11_audit_artifacts.py`를 추가했다.
- 검토 신호는 `docs/audits/artifact_audit.md`에 기록하고, 보상 ID 연결 검증과 교육적 사실 검토를 분리했다.
- 이번 단계에서는 유물명을 대량 변경하지 않았으며, 실제 역사 사실·문장 난이도 판정은 교사 검토 항목으로 유지했다.

## 2026-08-28 — 문서 기반 운영 체계 정비

- `PRD.md`, `BACKLOG.md`, `DECISIONS.md`를 최소 범위로 추가해 요구사항·미완료 검토·장기 설계 결정을 분리했다.
- `agents.md`, `README.md`, `project_context.md`에 문서별 역할과 링크를 연결하고, Regular MUD(5~10분 복습)와 Deep-dive MUD(확장 탐구)의 교육 목적을 명시했다.
- 이미 완료된 JSON 기반 MUD, Vanilla JS, GitHub Pages 구조와 교육과정 정합성 검토는 BACKLOG가 아니라 `DECISIONS.md`에 기록했다.
- `AI_Architecture_Reflection.md`는 저장소에 존재하지 않아 실행 지시로 해석하거나 내용을 보충하지 않았다. 실제 파일명은 `project_context.md`(소문자)이며 링크도 이 이름으로 유지했다.
- 검증: 핵심 문서 존재·링크 대상·작업 트리 상태를 확인하고, 문서 간 역할 및 완료/미완료 항목의 중복을 점검했다.

## 2026-08-28 — Regular MUD 시간 기준 보정

- 학생이 탭 가능한 요소를 의미 없이 빠르게 눌러 기존 5분 안팎의 활동을 1~2분 안에 끝내는 관찰을 반영했다.
- Regular MUD의 운영 기준을 10분 이내, 설계 목표 약 9분으로 수정하고, 자료 읽기·단서 확인·해석 없이 완료되는 경로를 별도 검증 대상으로 등록했다.
- Deep-dive MUD는 기존처럼 Regular의 대체가 아닌 확장 탐구 활동으로 유지하며 시간 기준을 혼합하지 않았다.

## 2026-08-28 — Claude/Codex 분업 후 P1 기준선 감사

- Claude 장면 영역과 충돌하지 않도록 28종 Regular MUD의 단계·시뮬레이터·완료 조건을 전수 확인했다.
- 기존 무결성·계약·카탈로그·시뮬레이터 런타임 검증은 모두 통과했다. 다만 다중 선택지 단계의 정답 위치가 1번에 치우쳤다는 기존 경고가 유지된다.
- 여러 게이지형 단계가 `target: 100`과 2~4회의 동일 탭으로 완료되므로, `required: true`만으로는 무작위 탭에 의한 1~2분 종료를 막지 못한다는 구조적 후보를 확인했다.
- 장면이 있는 단계의 `scene`, `hotspots`, `interaction`, `completion.target`, 단계 구조는 변경하지 않았다. 후속 수정은 장면이 없는 단계부터 우선 검토한다.

## 2026-08-28 — 무작위 탭 위험 자동 선별기 추가

- `scripts/09_audit_tap_resistance.py`를 추가해 Regular의 필수·비핫스팟 시뮬레이터 중 4회 이하의 반복 동작으로 완료되는 구조를 선별한다.
- 이 선별기는 실제 학생 소요 시간을 단정하지 않으며, 장면이 있는 단계에는 좌표 검토 필요 표시만 하고 JSON을 수정하지 않는다.
- 버튼형 시뮬레이터의 고유 `actions`는 반복 캔버스 탭과 구분해 선별 대상에서 제외한다.

## 2026-08-28 — 장면 비보유 Regular 메타데이터 기준 갱신

- `regular_joseon_economy.json`과 `regular_joseon_silhak.json`의 `playTime`을 기존 `5-10min`에서 현재 운영 기준인 `10min`으로 갱신했다.
- 두 파일 모두 `simulator.scene`, `hotspots`, `interaction`, `completion.target`, 단계 구조는 변경하지 않았다.

## 2026-08-28 — 실학 MUD 3단계 서사 정합성 보정

- `regular_joseon_silhak.json` 3단계와 3-1 실패 분기의 narrative가 동학·인내천 장면 및 infoText와 어긋나던 문제를 동학의 평등·사람의 존엄을 설명하는 내용으로 보정했다.
- `scene`, `hotspots`, `interaction`, `completion`, 단계 구조는 변경하지 않았다. 해당 JSON은 Claude의 Phase 37 장면 작업 파일이므로 narrative 변경 사실을 공유해야 한다.

## 2026-08-28 — Bronze 반복 탭 완료 조건 보강

- `mudEngine.js`에 선택형 시뮬레이터 액션의 고유 ID 집계와 `completion.uniqueActions` 판정을 추가했다.
- `regular_bronze_age.json` 2단계는 거석 운반의 경사로·굴림대·공동 노동 단서 3개를 각각 확인해야 선택지를 해제하도록 보강했다.
- 대상 단계에는 `scene`·`hotspots`가 없으며, Claude 담당 렌더러와 장면 계약은 변경하지 않았다.

## 2026-08-26 — 구석기 자연환경 탐색 개선

- 구석기 1단계에 `paleo-environment` 전용 시뮬레이터를 추가했다.
- 바위 그늘, 강가, 트인 평야를 서로 다른 탐색 지점으로 렌더링한다.
- 같은 지점을 반복해서 눌러도 완료되지 않으며, 3개의 서로 다른 환경 단서를 찾아야 선택지가 활성화된다.
- 탐색 지점별 역사적 피드백과 `환경 단서 찾기: n/3` 진행 표시를 제공한다.
- 불 피우기는 마른 풀 → 나뭇가지 → 부싯돌 순서를 판정한다.
- 주먹도끼 제작은 가장자리 → 돌의 면 → 뾰족한 끝의 서로 다른 작업 지점을 판정한다.
- 사냥은 발자국·바람·바위 길목, 공동체는 식량·불씨·도구·가죽을 각각 확인해야 한다.
- 마지막 성찰은 환경 관찰·도구와 불·공동체 협력의 세 단서를 확인해야 완료된다.
- 구석기 실패 분기 시뮬레이터도 해당 본 단계의 전용 활동을 재사용하도록 정리했다.
- 구형 `paleo-fire`·`paleo-stone` 렌더러가 전용 렌더러를 가로채던 분기 충돌을 제거했다.
- 불 피우기·주먹도끼의 단계 진행률을 화면 게이지와 연결하고, 안내 문구를 실제 상호작용 순서와 일치시켰다.
- 활동 판정 유형을 `interaction` 필드로 분리해 구석기 시뮬레이터가 공통 처리기(`hotspot-discovery`, `ordered-hotspot`, `resource-allocation`, `reflection`)를 사용하도록 전환했다.
- 시뮬레이터 활동 유형 문서를 `simulator_schema.md`에 정리하고 무결성 검증기에 유형 검사를 추가했다.
- MUD 무결성 검증기에 새 시뮬레이터 모드를 등록했다.
- 검증: `node --check` 2개 파일, 게임 데이터 검증, MUD 무결성 검증, `git diff --check`, 브라우저 반복 클릭·3개 단서 탐색 흐름.

## 반복 문제 점검 결과

- `paleo-fire`, `paleo-stone`, 일부 `culture-touch`·`text-reading` 활동은 여전히 좌표·선택·순서를 검증하지 않고 클릭 횟수로만 완료된다.
- 필수 시뮬레이터의 완료 계약이 모드별로 일관되지 않아 새 모드 추가 시 실행 코드·JSON·검증기를 함께 수정해야 한다.

## 2026-08-26 — 신석기 시뮬레이터 공통 계약 적용

- 신석기 1단계 환경 탐색을 `hotspot-discovery`로 전환했다.
- 신석기 2단계 빗살무늬 토기 제작을 `ordered-hotspot`으로 전환해 점토 → 뾰족한 바닥 → 빗살무늬 순서를 판정한다.
- 신석기 3단계 가락바퀴 옷감 짜기를 `resource-allocation`으로 전환해 가락바퀴·뼈바늘·실 재료를 각각 확인한다.
- 신석기 4단계 농경과 정착을 `reflection`으로 전환해 정착·농경·토기의 역사적 연결 단서를 확인한다.
- 위 활동의 핫스팟 좌표, 라벨, 피드백, 완료 조건을 `regular_neolithic.json`에 선언하고 공통 엔진 상태를 사용하도록 연결했다.
- 검증기에서 지원되는 `interaction` 유형을 검사하도록 보완했다.
- 브라우저 화면 검증은 로컬 브라우저가 구형 캐시 화면을 표시하고 런타임 전역 객체를 격리하는 문제로 정적 검증과 분리했다. 저장소 기준 검증은 계속 진행한다.

## 2026-08-26 — 선택지 버튼 구분성 개선

- 모든 선택지에 화면 표시 순번(`선택 1`, `선택 2` 등)을 부여해 무작위 섞기 이후에도 버튼을 식별할 수 있게 했다.
- 정답 여부를 색상이나 문구로 노출하지 않아 탐구형 선택의 비결정성은 유지했다.
- 각 버튼에 전체 선택지 내용을 포함한 `aria-label`을 추가해 보조기기 사용성을 보완했다.

## 2026-08-26 — 광복 MUD 필수 활동 연결

- 광복 1단계 태극기 시뮬레이터를 필수 활동으로 지정했다.
- 태극 문양과 건·곤·감·리 4괘를 모두 완성해야 다음 선택지가 열린다.
- 광복 3단계 5·10 총선거 시뮬레이터를 필수 활동으로 지정했다.
- 기표 도장 찍기 → 투표함 넣기의 두 행동을 완료해야 다음 선택지가 열린다.
- 중복 클릭은 행동 수를 중복 집계하지 않으며, 공통 `simulatorProgress` 완료 판정을 사용한다.
- 검증: JavaScript 문법 검사, 게임 데이터 검증, MUD 무결성 검사, `git diff --check`.

## 2026-08-26 — 삼국 MUD 한강 전략 탐색 연결

- 삼국 1단계에 한강 유역·서해 교역로·백제 영토 탐색 활동을 추가했다.
- 삼국 2단계에 평양성·한강 유역·국내성 탐색 활동을 추가했다.
- 삼국 3단계에 북한산 순수비·한강 유역·당항성 탐색 활동을 추가했다.
- 각 단계에서 3개의 역사적 지점을 확인해야 다음 결단이 열리도록 `hotspot-discovery`와 공통 완료 계약을 적용했다.
- 검증: JavaScript 문법 검사, 게임 데이터 검증, MUD 무결성 검사, `git diff --check`.

## 2026-08-26 — 인터랙션 데이터 계약 검증 강화

- 핫스팟이 선언된 시뮬레이터의 ID 중복, 좌표 범위, 라벨·피드백 누락을 무결성 검증에서 확인하도록 했다.
- `ordered-hotspot`의 순서 목록이 실제 핫스팟 ID를 참조하는지 검사하도록 했다.
- 필수 핫스팟 활동의 완료 목표가 실제 핫스팟 수를 초과하지 않는지 검사하도록 했다.

## 2026-08-26 — 청동기 고인돌 축조 활동 연결

- 고인돌 1단계 받침돌 세우기와 4단계 완성 행동을 필수 활동으로 연결했다.
- 2단계 흙 경사로는 진행도 100%까지 쌓아야 다음 결단이 열리도록 했다.
- 3단계 거석 견인은 동원 인원 슬라이더와 3회 이상의 이동 행동을 함께 요구하도록 했다.
- 기존 고인돌 전용 조작·렌더러를 유지하면서 공통 `simulatorProgress` 완료 판정을 연결했다.
- 검증: JavaScript 문법 검사, 게임 데이터 검증, MUD 무결성 검사, `git diff --check`.

## 2026-08-26 — 고조선 핵심 자료 탐색 연결

- 고조선 1단계에 요동·한반도 북부·아사달 건국 영역 탐색을 추가했다.
- 고조선 2단계에 8조법의 살인·상해·절도 조항 탐색을 추가했다.
- 고조선 3단계에 비파형 동검·고인돌·청동기 문화권 탐색을 추가했다.
- 각 단계에서 3개 역사 단서를 확인해야 다음 결단이 열리도록 공통 `hotspot-discovery` 완료 계약을 적용했다.
- 검증: JavaScript 문법 검사, 게임 데이터 검증, MUD 무결성 검사, `git diff --check`.

## 2026-08-26 — 통일신라 문화·통치 자료 탐색 연결

- 통일신라 1단계에 9주·5소경·왕권 강화 단서를 추가했다.
- 2단계에 석가탑·다보탑·청운교·백운교 탐색을 추가했다.
- 3단계에 석굴암 돔 쐐기돌·습도 조절·본존불 탐색을 추가했다.
- 각 단계에서 3개 역사 단서를 확인해야 다음 결단 또는 엔딩으로 진행하도록 공통 `hotspot-discovery` 완료 계약을 적용했다.
- 검증: JavaScript 문법 검사, 게임 데이터 검증, MUD 무결성 검사, `git diff --check`.

## 2026-08-26 — 구조 안정화 Phase 1 기준선 고정

- `main`과 `origin/main`이 `27124cd`로 일치함을 확인하고 MUD 32종·전체 242단계를 집계했다.
- 기존 JSON·MUD 무결성 검사와 핵심 JavaScript 문법 검사는 통과했으나, 원본 복수 선택지 115단계 모두 정답이 첫 위치임을 확인했다.
- GitHub Pages의 `_index.json`이 HTML로 응답하여 라이브 앱이 하드코딩 fallback으로 실행되는 배포 결함을 확인했다.
- iPad 10세대 세로 기준에서 2열 MUD와 피드백의 핫스폿 가림, 캔버스 대체 조작 부재를 재현했다.
- 상세 증거와 Phase 2 입력 결함은 `baseline_report_structural_stabilization.md`에 기록했다.

## 2026-08-26 — 구조 안정화 Phase 2 P0 활동·배포 봉쇄

- 불 피우기 legacy 상태에 `step`을 초기화하고 유한값 기반 진행률 계산을 적용해 오답 후 `NaN`이 발생하지 않도록 했다.
- 필수 hotspot 활동에 캔버스와 같은 행동을 수행하는 버튼 대체 조작을 추가했다.
- 피드백을 캔버스 밖으로 옮겨 iPad에서 남은 hotspot을 가리던 문제를 제거하고, 상태 알림을 추가했다.
- 앱 푸터에 배포 확인용 버전을 표시하고 `.nojekyll` 마커를 추가해 `_index.json`의 Pages 정적 제공을 준비했다.
- 검증: JS 문법, 게임/MUD 무결성, 로컬 iPad 820×1180에서 구석기 탐색·불 피우기 오답 후 재시도·완료, 콘솔 오류 0건, Pages `_index.json` HTTP 200 / `application/json`, 배포 버전 `2026.08.26-p2`.

## 2026-08-26 — Phase 3 데이터 계약·교육과정 매핑 기준선

- 현재 MUD 차시 번호가 2015 개정 교육과정 기준임을 `_index.json`에 명시하고, 목표 교육과정은 2022 개정으로 분리했다.
- `data/curriculum_mapping.json`에 32개 MUD의 기존 차시 연결을 보존하면서 2022 매핑은 `unmapped`·`pending-content-review`로 기록했다.
- `simulator_contract.json`과 `simulator_schema.md`에 지원 interaction과 필수 완료 필드의 기준을 문서화했다.
- `scripts/04_validate_mud_contract.py`를 추가해 인덱스·파일·매핑 수와 MUD ID, 시작 단계, simulator 계약을 자동 검증한다.
- README와 인수인계 문서의 교육과정·대시보드 범위를 실제 결정과 일치시켰다.
- 검증: 게임 데이터, MUD 무결성, 새 계약 검증, JS 문법 검사, `git diff --check` 통과. 기존 정답 1번 편중 경고는 후속 단계로 유지.

## 2026-08-26 — Phase 4 시뮬레이터 입력 경계 안정화

- `MudSimulators.interactionHandlers`와 dispatcher를 추가해 hotspot interaction별 처리기를 한 곳에서 선택하도록 했다.
- 캔버스 바깥 클릭, 이미 확인한 단서, 순서를 틀린 선택은 `simActionCount`에 반영하지 않도록 입력 판정과 행동 기록을 분리했다.
- 고인돌·투표·태극기 활동도 실제 상태 변화가 발생한 경우에만 행동을 기록하도록 정리했다.
- `resizeCanvas()`에서 Canvas transform을 초기화한 뒤 배율을 적용해 화면 재진입·회전 시 배경과 핫스팟이 누적 변형되지 않게 했다.
- 검증: `node --check` 2개 파일, MUD 무결성, simulator contract, `git diff --check` 통과.

## 2026-08-26 — Phase 4 runtime 상태·버튼 action 분리

- `MudEngine`에 simulator state·progress 접근자를 추가하고 JSON의 `progressKey`와 공통 진행률을 동기화했다.
- 화면상 진행과 완료 잠금이 다른 키를 읽던 구석기 6개 활동의 구조 결함을 수정하고 상태 단위 테스트를 추가했다.
- MUD JSON의 `buttonsHtml` 6개와 인라인 JavaScript를 구조화 action 14개로 전환했다.
- 광복 태극기의 전체 완성 단축 버튼을 제거하고 여섯 요소를 각각 수행해야 완료되도록 조정했다.
- iPad 820×1180 브라우저 검증에서 구석기 3개 단서 후 잠금 해제, 광복 5개 후 잠금 유지·6개 후 해제, 콘솔 오류 0건을 확인했다.

## 2026-08-26 — Phase 5 정적 품질 게이트

- `.github/workflows/quality-gate.yml`을 추가해 push·pull request에서 JSON, MUD 계약, runtime 단위 테스트, 정적 asset, JS 문법, whitespace를 검사한다.
- `scripts/06_validate_static_assets.py`로 index.html의 로컬 참조 파일과 `.nojekyll` 존재를 확인한다.
- README의 검증 불가능한 100%·완벽 표현을 현재 기준선과 지원 범위 표현으로 조정했다.
- 로컬 품질 게이트 전체 검증 통과. 기존 선택지 정답 1번 편중 경고는 별도 콘텐츠/랜덤화 과제로 남아 있다.

## 2026-08-26 — Phase 6 선사·고조선 2022 파일럿 매핑

- 구석기·신석기·청동기·고조선 MUD 4종을 2022 개정 성취기준 `[6사04-01]`에 직접 매핑했다.
- 각 MUD에 교육과정 메타데이터와 국립특수교육원·국사편찬위원회 출처를 기록했다.
- 전곡리 연대 혼동, 신석기 환경 단정, 청동기 현대 지리 용어, 고조선 위치·건국 연대의 확정적 표현을 교정했다.
- Deep-dive·스토리·연표에 반복되던 기원전 2333년 확정 표현도 건국 전승으로 통일했다.
- 출판사별 단원명·차시 번호는 교과서 정보가 확인되기 전까지 비워 두고, 성취기준 매핑만 완료 상태로 관리한다.

## 2026-08-26 — Phase 7 성취기준 전수 분류

- `data/curriculum_standards_2022.json`에 고대·고려·조선·식민지기/광복·전쟁·민주화/산업화의 2022 개정 성취기준을 추가했다.
- 32개 MUD 중 30개를 `[6사04-01]`~`[6사07-02]`에 직접 매핑하고, 해당 MUD JSON과 매핑표의 성취기준이 일치하도록 계약 검증을 확장했다.
- `regular_myeongnyang`, `regular_joseon_diplomacy`는 현행 성취기준과의 직접 정합성이 약하므로 보조 역사 맥락으로 표시했다. 삭제하거나 억지로 재매핑하지 않는다.
- 출판사별 단원·차시 번호와 MUD별 역사 서술의 근거 출처는 다음 단계에서 검증한다.

## 2026-08-26 — Phase 8 역사 서술 1차 감사

- `content_claim_audit.md`에 반복 문제와 보정 원칙을 기록했다.
- 훈민정음·학익진·석굴암·칠정산·앙부일구·고려 금속활자·전곡리 주먹도끼·대한민국 임시정부·명량대첩의 과도한 절대 표현을 자료 중심 표현으로 바꿨다.
- 이번 단계는 서술 위험을 낮추는 1차 보정이며, 모든 개별 주장에 대한 공식 출처 대조는 다음 단계로 남겼다.

## 2026-08-26 — Phase 9 주제별 역사 출처 메타데이터 보강

- 직접 매핑된 MUD에 2022 개정 교육과정 출처와 국사편찬위원회 주제 자료 출처를 함께 기록했다.
- 계약 검증은 직접 매핑 MUD에 교육과정 출처와 역사 출처가 모두 있는지 확인하도록 강화했다.
- 주제 자료는 개별 문장 자동 승인이 아니라 후속 세부 사실 검증을 위한 기준점으로 명시했다.

## 2026-08-27 — Phase 10 활동 시간·상호작용 전수 감사

- `scripts/07_audit_activity_duration.py`와 `activity_duration_audit.md`를 추가해 Regular MUD의 핵심 단계·능동 활동·필수 활동·예상 시간을 전수 계산한다.
- Regular 28종 중 24종이 보완 대상으로 확인되었고, 가장 반복적인 구조 결함은 시뮬레이터 완료 없이 선택지를 통과할 수 있다는 점이다. 구석기는 자동 추정상 10분을 조금 넘을 가능성이 있어 실제 학생 시간 측정 대상으로 표시했다.
- Regular MUD 28종의 선언 시간을 승인된 개인 활동 목표인 `5-10min`으로 통일했다.
- iPad 10세대 가로 크기(1180×820)에서 구석기 화면의 레이아웃, 필수 활동 전 선택지 잠금, 대체 터치 버튼 상태를 확인했다.
- 품질 게이트에서 감사 보고서가 데이터와 동기화되어 있는지 검사한다. 실제 5~10분 판정은 학생 3명 이상의 사용 시간 측정으로 확정한다.

## 2026-08-27 — Phase 11 레거시 터치 활동 잠금 전환

- `culture-touch`, `text-reading`, `battle-gauge`, `economy-*` 66개 단계에 두 번의 조작과 100% 진행률을 요구하는 공통 완료 조건을 적용했다.
- 이 모드들은 이미 공통 입력 처리에서 진행률을 올리고 있어 별도 렌더러를 추가하지 않고 선택지 잠금을 적용할 수 있었다.
- 발해 MUD의 실제 화면에서 조작 전 선택지 비활성화, 두 번 터치 후 선택지 활성화, 콘솔 오류 없음까지 확인했다.
- 활동 감사 기준상 보완 대상은 24종에서 19종으로 줄었다. 전용 `mn-*` 모드와 핵심 단계가 3개인 MUD는 별도 설계 단계로 남긴다.

## 2026-08-27 — Phase 12 명량 전용 상호작용 완료 조건

- 명량 본선 1·2단계는 전력·지형 단서 세 가지를 각각 확인해야 결단할 수 있도록 구조화 `observe` 행동을 추가했다.
- 3단계는 실제 왜선 네 척 명중과 네 번 이상의 조작을, 4단계는 조류 슬라이더 80 이상 조작을 완료 조건으로 연결했다.
- 포탄 충돌 시 완료 판정을 즉시 갱신하고, 슬라이더의 사용자 입력만 행동으로 세도록 엔진 경계를 보강했다.
- 자동 runtime 테스트로 중복 관찰 무효화와 슬라이더 임계값을 고정했으며, 실제 화면에서 세 단서 확인 전 잠금·확인 후 해제와 콘솔 오류 0건을 확인했다.
- 활동 감사 기준상 보완 대상은 19종에서 18종으로 줄었다. 다음 단계는 핵심 단계가 3개인 MUD의 시대별 내용·분기 보강이다.

## 2026-08-27 — Phase 13 고대사 3단계형 MUD 확장 파일럿

- 통일신라와 발해를 3개에서 4개 핵심 단계로 늘리고, 마지막에 유적·문서·제도·주민 자료를 비교·종합하도록 했다.
- 통일신라의 석굴암 습도 조절 단정과 발해의 단일 민족·단일 문화 서사를 자료가 뒷받침하는 범위로 교정했다.
- 터무니없는 오답 대신 한 자료만으로 시대 전체를 단정하는 실제적인 오개념을 실패 분기로 만들었다.
- 국사편찬위원회 자료를 다시 대조해 출처 범위를 기록했고, 두 MUD의 예상 활동 시간은 7.5분·7.9분이 되었다.
- iPad 크기 실제 화면에서 두 MUD의 새 4단계 완주와 콘솔 오류 0건을 확인했으며, 보완 대상은 18종에서 16종으로 줄었다.

## 2026-08-27 — Phase 14 일제 강점기 3단계형 MUD 확장 파일럿

- 3·1 운동·임시정부와 1910년대 무단 통치 MUD에 선언·시위·헌장, 통치·수탈·저항 자료를 종합하는 4단계를 추가했다.
- 독립운동을 단일 영웅의 서사로 좁히거나 토지 조사 사업의 결과를 근거 없는 숫자로 단정하던 표현을 자료 중심으로 교정했다.
- 모든 실패 분기는 한 자료로 시대 전체를 판단하는 오개념을 다시 비교하도록 바꿨다.
- 국사편찬위원회 헌병 경찰·토지 조사 사업·대한민국 임시 헌장 자료의 적용 범위를 MUD 메타데이터에 기록했다.
- 실제 화면 완주과 콘솔 오류 0건을 확인했고, 활동 감사의 보완 대상은 16종에서 14종으로 줄었다.

## 2026-08-27 — Phase 15 고려 3단계형 MUD 확장 파일럿

- 거란 침입과 고려 문화·교류 MUD에 외교·방어·재건, 제작·인쇄·교류 자료를 종합하는 4단계를 추가했다.
- 서희 담판의 조건과 이후 축성 과정, 귀주대첩 뒤 재건을 반영하고 전쟁의 인명 피해를 승리 놀이처럼 다루던 표현을 교정했다.
- 팔만대장경·직지·벽란도는 각 자료가 알려 주는 범위와 한계를 구분하도록 바꿨다.
- 국사편찬위원회 강동 6주·귀주대첩 자료를 추가 기록했고, 공통 interaction·계약·무결성 검증을 통과했다.
- 활동 감사의 보완 대상은 14종에서 12종으로 줄었다. 다음 묶음은 조선 3단계형 MUD이다.

## 2026-08-27 — Phase 16 조선 3단계형 MUD 확장 파일럿

- 조선 후기 서민 문화, 조선 신분제, 세종 시기 과학 MUD에 그림·공연·문학, 법제·직업·호적, 문자·관측·기구 자료를 종합하는 4단계를 추가했다.
- 조선 신분을 고정된 네 계층으로 단순화하던 서술을 법제상 양천제와 실제 계층·시기별 변화로 보정했다.
- 세종 한 사람의 영웅 서사는 정책·학자·기술자의 활동과 자료의 범위로, 서민 문화는 단일한 저항 서사가 아닌 다양한 공연·문학·생활 자료로 바꿨다.
- 국사편찬위원회 초등·교과서 자료의 적용 범위를 기록했고, 활동 감사의 보완 대상은 12종에서 9종으로 줄었다.

## 2026-08-27 — Phase 17 근현대 3단계형 MUD 확장 파일럿

- 개항기, 일제 말기, 전후 사회 MUD에 조약·시설·도시 생활, 정책·피해 기록·언어 운동, 피해·재건·분단 자료를 종합하는 4단계를 추가했다.
- 근대 문물을 무조건적 발전으로, 전후 재건을 개인의 노력만으로, 평화를 개인의 다짐만으로 설명하던 단선을 자료 비교로 보정했다.
- 강제 동원과 일본군 위안부 피해는 선정적 재현 대신 기록·증언의 의미와 피해자 존엄을 존중하는 학습 흐름으로 수정했다.
- 국사편찬위원회 강화도 조약·민족 말살 정책·전후 사회 자료의 적용 범위를 기록했고, 활동 감사의 보완 대상은 9종에서 6종으로 줄었다.

## 2026-08-27 — Phase 18 민감한 역사 활동의 정서 안전성 재설계

- 한국전쟁 MUD의 전투 수행·학도병 영웅화와 의병/안중근 MUD의 자해·총격 재현을 제거하고 두 활동을 각각 4단계 자료 탐구로 재설계했다.
- 한국전쟁은 전쟁 발발·피난, 전선 지도와 학생 일기, 정전 협정, 피해·분단·평화 종합으로 구성해 승패보다 사람들의 경험과 평화 과제를 살피게 했다.
- 의병/안중근은 의병의 다양한 참여, 단지동맹 태극기 관찰, 하얼빈 의거와 재판 진술 구분, 동양평화론 종합으로 구성했다.
- 위험한 역사적 사실은 숨기지 않되 학생이 모방하지 않도록 명시하고, 모든 단계에서 세 자료 단서를 직접 찾아야 선택할 수 있게 했다.
- 국사편찬위원회 자료를 출처 메타데이터에 추가했으며, 활동 감사의 보완 대상은 6종에서 4종으로 줄었다.

## 2026-08-27 — Phase 19 남은 수동 활동 구조 보완

- 고조선과 조선 건국 MUD의 실제 단서 탐색 화면이 감사에서 수동 정보 화면으로 잘못 집계되던 문제를 터치 기반 모드로 정리했다.
- 두 MUD의 마지막 단계도 건국 전승·법·유물, 한양의 입지·도성·교육 기관을 연결하는 세 단서 탐색으로 바꿨다.
- 조선 외교 MUD는 국경 정책, 병자호란의 성 안팎 기록, 전란 뒤 외교·회복을 자료로 읽는 네 단계 흐름으로 확장했다.
- 승패·성벽 방어 중심의 선택지를 자료의 범위와 백성의 생활을 함께 해석하는 선택지로 교체했다.
- 무결성·계약·런타임 검증을 통과했고, 활동 감사의 보완 대상은 4종에서 구석기 1종으로 줄었다.

## 2026-08-27 — Phase 20 구석기 활동 시간 최적화

- 구석기 핵심 생존 판단 5개는 필수 단서 탐색으로 유지하고, 마지막 성찰은 선택형 회고로 전환했다.
- 첫 보금자리와 사냥 단계의 중복 설명을 압축해 학습 개념과 단서 피드백은 유지하면서 읽기 부담을 줄였다.
- 구석기 활동을 5~10분 목표에 맞추고 전체 Regular MUD 활동 감사의 보완 신호를 제거했다.

## 2026-08-27 — Phase 21 iPad 화면·터치 흐름 점검

- iPad 10세대와 같은 820×1180 화면에서 구석기 환경 탐색의 선택지 잠금·세 단서 확인 뒤 해제·콘솔 오류 0건을 확인했다.
- 6·25 전쟁 MUD에서도 잠금과 단서 확인 뒤 해제가 정상 동작했으나, 처음부터 완료 문구가 보이는 데이터 표현 문제를 발견했다.
- 완료 문구를 초기 안내 문구와 함께 재사용하던 14개 단계를 전수 탐지해, 시작 시에는 탐색 안내가 보이고 완료 뒤에만 성공 문구가 보이도록 교정했다.
- MUD JSON과 진도표의 응답을 캐시 없이 읽도록 해 배포 뒤 예전 시나리오가 보일 위험을 줄였고, 진도표·README의 변경된 세 시나리오 제목도 실제 JSON과 맞췄다.

## 2026-08-27 — Phase 22 MUD 카탈로그 자동 정합성 검증

- `_index.json`의 제목·부제·차시·테마·스토리 정보가 각 MUD JSON과 같은지 검사하는 전용 검증을 추가했다.
- 새 검증이 선사 심화 MUD 부제와 3·1 운동 MUD 제목의 실제 불일치를 찾아 즉시 교정했다.
- README에 32개 MUD의 기계 검증되는 전체 목록을 추가해, 진도표와 문서의 기준 정보를 한 곳에서 확인할 수 있게 했다.

## 2026-08-27 — Phase 23 2022 개정 성취기준 정합성 검증

- 국립특수교육원 성취기준·성취수준 원문과 32개 MUD 매핑을 다시 대조해 직접 매핑 30종과 보조 맥락 2종을 확정했다.
- 파일럿으로 남아 있던 선사·고조선 4종도 정식 성취기준 매핑 상태로 전환했다.
- 성취기준 검증 완료와 출판사·교과서 판본별 차시 확인 대기를 별도 상태로 분리해 과도한 확정을 방지했다.
- 성취기준별 적용 범위와 보조 활동의 활용 원칙을 별도 보고서에 정리했다.

## 2026-08-27 — Phase 24 역사 출처 메타데이터 품질 게이트

- 32개 MUD의 출처 필수 필드, URL 형식, MUD 내부 중복, 교육과정·국사편찬위원회 출처 구성을 자동 검사하는 품질 게이트를 추가했다.
- 반복 실행 과정에서 누적된 안중근 MUD 출처 2건과 한국전쟁 MUD 출처 4건을 제거했다.
- 민감 역사 MUD 생성 스크립트도 URL 기준으로 출처를 한 번만 추가하도록 바꿔 같은 문제가 재발하지 않게 했다.
- 출처가 없던 명량·조선 외교 보조 MUD에는 교육과정상 보조 활동이라는 한계와 국사편찬위원회 역사 근거를 함께 기록했다.

## 2026-08-27 — Phase 25 역사 서술 품질 1차 감사

- 과장 표현, 반사실적 단정, 단일 원인, 집단 비하·폭력 재현을 A~D 등급으로 구분하는 감사 기준을 세웠다.
- 일제 말기 MUD는 원고 하나의 유실이 한국어·국가 소멸로 이어진다는 단정을 자료의 의미와 한계를 비교하는 문장으로 바꿨다.
- 전후 재건은 개인의 끈기만이 아니라 정책·산업·국제 원조 등 복합 조건을 살피게 했고, 세종의 칠정산·측우기 설명은 확인 가능한 범위로 제한했다.
- 명량의 가상 패망과 극적인 궤멸 서술을 해상 보급·선박·화포·물길 자료를 연결하는 판단 활동으로 조정했다.

## 2026-08-27 — Phase 26 Deep-dive 폭력·집단 서사 재설계

- Deep-dive 4종의 몰살·섬멸·국가 소멸·집단 비하형 문장을 전수 분류하고 D급 핵심 표현을 제거했다.
- 선사 활동에서 학생이 포로 노비화와 가혹한 처벌을 명령하던 역할을 자료에서 계층과 법의 성격을 해석하는 조사자 역할로 전환했다.
- 삼국·조선 전쟁은 상대 집단을 공격하는 체험 대신 지형·행정 기구·선박·화포·보급 자료를 연결하도록 바꿨다.
- 근현대사는 한국광복군·낙동강 전선 하나로 이후 역사를 확정하지 않고 광복의 복합 원인과 전쟁 속 개인 기록을 함께 읽도록 조정했다.

## 2026-08-27 — Phase 27 Regular 실패 분기 정규화

- 25개 Regular MUD에서 실패 화면의 `역사의 IF` 표기를 `자료 해석`으로 일괄 전환했다.
- 일제 피해 증언, 중인 전문 기술, 상민·노비 노동, 개항기 교통은 한 자료나 한 집단이 사회 전체를 결정한다는 단정을 자료의 범위와 복합 조건을 살피는 피드백으로 바꿨다.
- 도구·지형·유물 선택의 조건을 설명하는 피드백은 유지해, 게임의 즉각적인 학습 안내와 역사적 가상 결말을 구분했다.

## 2026-08-27 — Phase 28 `최초`·수치 주장 사실 범위 감사

- 국사편찬위원회 자료와 대조해 매소성 20만은 『삼국사기』 기록과 과장 가능성을 함께 제시하고, 명량은 전체 함대·교전선·격파 수를 구분했다.
- 명량 제목도 `13척으로 133척을 막아선 기적`에서 `13척과 울돌목의 물길`로 바꿔 수치 경쟁보다 지형·선박 자료에 초점을 맞췄다.
- 모내기 노동력 1/5, 전차 아시아 최초, 현재 세계 경제 순위처럼 근거 범위가 불명확하거나 변동하는 표현을 제거했다.
- 상정고금예문은 기록상 금속활자 사용과 실물 미전승을 구분하고, 고조선 50만 전은 화폐의 보편 사용을 확정하지 않도록 조정했다.

## 2026-08-27 — Phase 29 집단 호칭·가치 편향 감사

- 학생 안내 문장의 `왜군`, `까막눈`, `미신`을 각각 일본군, 문해 환경, 자료 근거를 설명하는 중립적 표현으로 정규화했다.
- 성리학과 실학을 낡음/진보로 단순 대립시키거나 독립운동을 `민족의 혼`으로만 설명하던 문장을 다양한 자료와 관점을 비교하도록 교정했다.
- 모욕적인 오답 선택지와 `구국의 명장`, `위대한 민족 예술` 같은 정답 유도 표현을 직책·사료·관찰 가능한 특징 중심으로 바꿨다.
- Deep-dive 4종 엔딩을 진/노멀/배드의 국가 가치 평가에서 자료 종합·부분 해석·재검토의 학습 성취 표현으로 전환했다.

## 2026-08-27 — Phase 30 iPad 시뮬레이터 UX 회귀 점검

- iPad 10세대 세로 화면(820×1180)에서 구석기·고조선 MUD의 선택 잠금, 단서 탐색, 선택 해제와 콘솔 오류 0건을 실제 브라우저로 확인했다.
- 구석기 첫 활동은 세 장소를 모두 확인해야 완료되는 조건과 안내 문구가 달랐으므로, 세 단서를 명시하도록 바로잡았다.
- 모든 핫스팟 활동이 공유하는 대체 조작 버튼에 최소 44px 높이를 적용해 iPad 터치 목표 크기를 충족시켰다.
- 불 피우기에서 순서를 틀려도 재료 버튼이 잠기지 않고 올바른 첫 재료부터 다시 진행되는 것을 확인했다.

## 2026-08-27 — Phase 31 구석기 시뮬레이터 장면 맥락화

- 구석기 첫 활동의 한탄강 지형 장면을 기준으로, 불·주먹도끼·사냥·공동체·성찰 활동에도 내용과 맞는 캔버스 배경을 추가했다.
- 동굴의 모닥불과 재료, 자갈밭과 석기, 평원·동물 흔적·바위 길목, 야영지와 나눔, 해 질 녘 성찰 장면이 각각의 단서 위치와 함께 보인다.
- 외부 이미지 대신 코드 기반 벡터 장면을 사용해 저작권 확인, 네트워크 의존, iPad 로딩 부담 없이 일관된 화면을 유지한다.
- iPad 10세대 세로 기준에서 시각 점검과 단서 완료 흐름, 44px 대체 조작, 콘솔 오류 0건을 다시 확인했다.

## 2026-08-27 — Phase 32 신석기·고조선 장면 구조 확장

- 공통 핫스팟 렌더러가 `culture-touch` 모드만 보고 단색 장면을 그리던 문제를 해결하기 위해, 각 시뮬레이터 JSON의 `scene` 키를 읽도록 확장했다.
- 신석기 4단계에는 한강변 움집 마을·토기 제작·옷감 제작·정착 성찰 장면을, 고조선 4단계에는 영역 지도·8조법 기록·동검과 고인돌·자료 종합 장면을 연결했다.
- 각 장면은 실제 단서 좌표를 가리지 않고, 핫스팟 원형과 대체 조작을 그대로 공유하므로 상호작용 계약은 변경하지 않았다.
- iPad 10세대 세로 화면에서 신석기와 고조선의 장면·44px 터치 대상·선택 잠금·콘솔 오류 0건을 점검했다.

## 2026-08-27 — Phase 33 삼국·통일신라·발해 장면 맥락화

- `scene` 키를 필수 핫스팟 활동 8곳에 추가했다: 삼국 백제(한강·서해 교역로·영토), 고구려(평양성·국내성·남진 경로), 신라(북한산 순수비·한강·당항성), 통일신라 9주 5소경 행정 기록·불국사(석가탑·다보탑·청운교백운교)·석굴암(돔·불상 배치·본존불)·통치문화교류 자료 종합, 발해 유적·문서·주민 구성 자료 종합.
- `required: true`이지만 `hotspots`가 없는 게이지형 활동(삼국 4단계, 발해 1~3단계)은 공통 핫스팟 렌더러 경로를 타지 않으므로 이번 범위에서 제외했다.
- 삼국 세 장면은 정밀 지도가 아닌 모식도로 그려 방위·축척을 표시하지 않았고, 영토는 실선이 아닌 파선으로 표현해 확정된 국경으로 오독되지 않게 했다. 발해 주민 구성 장면은 집 크기와 높이를 동일하게 배치해 집단 간 우열로 읽히지 않도록 했다.
- 핫스팟 좌표·완료 조건·피드백 문구는 변경하지 않았고, 캔버스 렌더러 스모크 테스트로 8개 장면이 예외 없이 그려지고 `setLineDash` 상태가 항상 복원되는지 확인했다. 실제 브라우저 시각 점검은 Codex가 수행한다.

## 2026-08-27 — Phase 34-0 deep-dive mode 오배정 버그 수정

- `deep_joseon`(2·7), `deep_modern`(1·3·5), `deep_three_kingdoms`(1·7), `deep_prehistoric`(1a) 8개 단계가 훈민정음 창제·수원 화성 축조·만세 운동·낙동강 방어선 등 실제 내용과 무관하게 `mode: "paleo-fire"`/`"paleo-stone"`을 재사용하고 있었다. `drawSim()`과 `handleCanvasTouch()` 어느 분기에도 걸리지 않아 캔버스가 비고 게이지가 0%에서 멈추는 상태였다.
- 각 단계의 `mode`만 내용에 맞는 `culture-touch` 또는 `battle-gauge`로 교체했다. 핫스팟·완료 조건·안내 문구·렌더러 코드는 손대지 않았다.
- Node 스텁으로 `handleCanvasTouch()`를 직접 호출해 8개 모두 `gaugeProgress`가 실제로 증가하는지 확인했다. 전부 `required: false`(선택 활동)이므로 진행을 막지는 않았지만 화면이 반응하지 않는 상태였다.
- 이 수정은 화면을 "고장 → 제네릭"으로 되돌릴 뿐 시대 맥락 장면을 주지는 않는다. 장면 추가는 후속 Phase의 범위다.

## 2026-08-27 — Phase 34A 자료 종합형 11종 장면 확장

- 고려 문화·항쟁, 독립운동, 일제강점기 1·2, 조선 서민 문화·건국·신분제, 근대 개항, 광복 이후, 세종 등 11개 MUD의 "세 자료를 비교하세요" 4단계에 `scene` 키를 추가했다. 전부 서로 다른 시대인데도 동일한 초록 원 placeholder로 렌더링되던 문제를 해결했다.
- `unified-silla-summary`·`balhae-evidence`에서 쓰던 자료 카드 3장 레이아웃을 그대로 계승해, 새 화풍을 만들지 않고 시각적 일관성을 유지했다. 공통 카드 그리기를 `drawConfiguredSceneBackground` 내부의 `card()` 헬퍼로 뽑아 11개 장면이 공유한다.
- 카드 안 도형은 팔만대장경판·직지·벽란도 교역선(고려 문화)부터 훈민정음 해례본의 천지인 기하 기호·칠정산 궤도·앙부일구(세종)까지, 각 MUD의 실제 핫스팟 자료와 1:1로 대응한다.
- 민감 시대(일제강점기 1·2)는 봉인·문서·장부 등 중립적 기호로만 표현했고, 사람·가해 집단·폭력 묘사를 넣지 않았다.
- 캔버스 렌더러 스모크 테스트로 11개 장면 전부 예외 없이 그려지고 `setLineDash`가 복원되는지, JSON-JS `scene` 문자열이 27개 전부 일치하는지 확인했다. 실제 브라우저 점검은 Codex가 수행한다.

## 2026-08-27 — Phase 34B 서사형 3종 장면 확장(민감 시대 표현 원칙 적용)

- `regular_independence_army`(1~4), `regular_joseon_diplomacy`(1~4), `regular_korean_war`(1~4) 12개 단계에 `scene`을 추가했다. 다단계 서사 구조라 34A의 자료 카드 패턴 대신 단계별 전용 장면을 새로 설계했다.
- 단지동맹 태극기(2단계)는 신체 훼손을 연상시키지 않도록 작업대·조명·확대경·보호 장갑으로 구성한 유물 보존실 관찰 장면으로 그렸다. 신체·혈흔 묘사는 전혀 넣지 않았다.
- 6·25전쟁 지도(1·3단계)는 남북을 동일한 회색 반도 실루엣으로 그리고 38선·군사분계선을 중립적인 회색 파선으로만 표현했다. 정전 협상 탁자는 좌우 대칭 구성으로 그려 승패를 암시하지 않았고, 깃발·비둘기 등 상징을 넣지 않았다.
- 남한산성 전란(2단계)은 성곽 윤곽·눈 결정·빈 항아리로 겨울 농성의 어려움을 표현했고, 전투·병사·무기는 그리지 않았다. 이산가족·강제 동원과 마찬가지로 사람의 고통을 직접 묘사하지 않는 원칙을 유지했다.
- 캔버스 렌더러 스모크 테스트로 12개 장면 전부 예외 없이 그려지고 `setLineDash`가 복원되는지, JSON-JS `scene` 문자열이 39개 전부 일치하는지 확인했다. 실제 브라우저 점검은 Codex가 수행한다.

## 2026-08-27 — Phase 35 렌더러 확장과 고대 잔여 8종

- `drawSim()`에 3줄을 추가해, 핫스팟이 없는 게이지형 활동도 JSON에 `scene`을 선언하면 시대 배경을 그리도록 확장했다. `scene`을 선언하지 않은 기존 활동의 분기 순서는 전혀 바꾸지 않았다.
- `regular_three_kingdoms`(4), `regular_three_kingdoms_life`(1~4), `regular_balhae`(1~3) 8개 필수 게이지형 활동에 `scene`을 추가했다. 이 8개는 핫스팟이 없어 기존에는 `scene`을 선언해도 무효였던 활동이다.
- 가야 철제 판갑옷(3단계)은 mode 이름이 `battle-gauge`이고 안내 문구가 "탐구"라 쓰였지만 실제 내용은 제철·갑옷 제작 기술이므로 전투 없이 공방(가마·판갑옷·덩이쇠)으로 그렸다. 발해 건국(1단계)도 "결전" 문구 대신 산세·깃발·이동 경로만으로 표현해 병사·무기 묘사를 피했다.
- **회귀 검증이 이번 Phase의 핵심이다.** 실제 `drawSim()`을 호출하는 Node 테스트로, `scene`을 선언하지 않은 5개 대표 레거시 mode(`gwangbok-flag`, `economy-farm`, `text-reading`, `battle-gauge`, `culture-touch`)가 여전히 원래 placeholder 문구를 그대로 그리고 `drawConfiguredSceneBackground`를 전혀 호출하지 않는지 확인했다. 동시에 `scene`을 선언한 게이지 활동은 새 분기로 정상 라우팅되는지도 확인했다.
- 캔버스 렌더러 스모크 테스트로 8개 장면 전부 예외 없이 그려지고, JSON-JS `scene` 문자열이 47개 전부 일치하는지 확인했다. 실제 브라우저 점검은 Codex가 수행한다.

## 2026-08-27 — Phase 36 고려 4종 14개 장면 확장

- 고려 건국·사회·항쟁·문화 MUD의 필수 활동 14개에 scene 키와 캔버스 장면을 연결했다.
- 후삼국 모식도·발해 유민 수용·훈요 10조·과거·균등 상속·배흘림기둥·외교 담판·경판·금속활자·벽란도 항구 등 자료와 활동을 연결했다.
- 흥화진·귀주는 전투 승리 대신 지형·방어 거점·재건 단서로, 벽란도는 세계지도 대신 한 항구의 배·창고·화물로 표현했다.
- JSON·분기 계약·런타임 검증을 통과했으며, 실제 iPad 육안 점검은 후속 검증 대상으로 남긴다.
## 2026-08-31 — P2-03 MUD 등록 경로 구조 감사

- `_index.json` 32개 등록 항목과 커리큘럼 48차시의 매칭 상태를 대조했다.
- Regular MUD 28종 중 포털 매칭 차시는 39개이며, 도입·정리 차시 등 9개는 MUD가 없다.
- `app.js`의 레거시 Regular 조건문이 약 140줄 남아 있음을 확인했다.
- `unitId + lessonNumbers`만으로는 2단원 7차시와 3단원 12차시가 각각 두 MUD와 중복되어 첫 배열 항목을 선택하는 문제가 있다.
- `docs/plans/implementation_plan_registration_single_source.md`를 작성하고, `primary`/`supplementary` 등록 계약과 검증 선행 원칙을 기록했다.
- 이번 묶음에서는 코드와 인덱스 데이터를 변경하지 않았다. 주·보조 MUD의 교육적 선정이 먼저 필요하다.

## 2026-08-31 — P2-03 인덱스 주·보조 계약 1차 반영

- `regular_myeongnyang`과 `regular_korean_war`를 `supplementary`로 표시해 중복 차시의 기본 포털 버튼에서 제외했다.
- `app.js`의 인덱스 매칭이 `supplementary` 항목을 건너뛰도록 수정했다.
- `scripts/03_validate_mud_integrity.py`에 placement 유효성, MUD ID 중복, primary 차시 중복 검사를 추가했다.
- 검증 통과: MUD 무결성, MUD 계약, 정적 자산, 시뮬레이터 런타임, `node --check js/app.js`, `git diff --check`.
- `app.js`의 기존 Regular 레거시 조건문은 아직 남아 있어, 다음 묶음에서 인덱스 실패 시에만 제한적으로 유지하거나 제거한다.

## 2026-08-31 — P2-03 포털 Regular 매칭 단일 경로 1차 완료

- `app.js`의 기존 Regular 차시 조건문을 `_index.json` 로드 실패 시에만 실행되는 비상 호환 경로로 격리했다.
- 인덱스가 정상 로드되면 primary MUD는 인덱스 매칭만 사용하고, 인덱스에 없는 차시는 퀴즈·탐구·준비 중 분기로 처리한다.
- 새 Regular MUD는 JSON 파일과 `_index.json`만 갱신하면 포털 매칭 대상이 되는 구조를 확인했다.
- 검증 통과: MUD 무결성, MUD 계약, 정적 자산, 시뮬레이터 런타임, `node --check js/app.js`, `git diff --check`.
- 남은 확인: 보조 MUD 별도 노출, 실제 브라우저·태블릿에서 2단원 7차시와 3단원 12차시 버튼 확인.

## 2026-08-31 — P3 브라우저·기기 회귀 점검 시도

- 브라우저 연결을 시도했으나 현재 클라이언트가 설치되지 않은 `26.825.31414` 서비스 파일을 참조해 초기화에 실패했다.
- 브라우저 도구 문제로 실제 화면·태블릿 육안 검증은 수행하지 못했다.
- 코드 기반 검증은 계속 통과했으며, 브라우저 도구가 정상화되면 2단원 7차시·3단원 12차시의 primary 버튼과 대표 MUD 흐름을 우선 확인한다.

## 2026-08-31 — P3 브라우저 회귀 체크리스트 문서화

- 브라우저 연결 복구 후 바로 실행할 수 있도록 `BROWSER_REGRESSION_CHECKLIST.md`를 추가했다.
- 포털 초기 화면, 중복 차시 매칭, 대표 MUD 흐름, 터치·접근성, 기록 양식을 한 문서에 고정했다.
- 핵심 흐름 차단·잘못된 MUD 연결·터치 실패의 우선순위 판정 규칙도 기록했다.

## 2026-09-01 — P1-02 반복 탭 내성 브라우저 회귀 1차

- 로컬 Chrome에서 `regular_independence` 1단계를 열어 대체 조작 버튼을 확인했다.
- 같은 단서를 다시 누르면 버튼이 즉시 비활성화되고 진행률이 1/3에 머물러, 반복 탭만으로 완료되지 않음을 확인했다.
- 세 단서를 모두 확인하기 전 선택지가 잠기고, 모두 확인한 뒤 선택지가 활성화되는 것을 확인했다.
- 오답 선택 후 `1-1` IF 단계로 이동하고, 재시도 버튼으로 1단계에 복귀하며 단서 상태가 초기화되는 것을 확인했다.
- 이번 점검은 1단계 대표 흐름만 대상으로 했으므로 2~3단계 전체 정상·오답·IF 회귀 확인은 남겨 두었다.

## 2026-09-01 — P1-02 2~3단계 브라우저 회귀 완료

- `regular_independence`에서 1→2→3단계 정상 진입을 확인했다.
- 2단계는 단서 3개 확인 전 선택지 잠금, 완료 후 활성화, 오답→`2-1` IF→재시도→2단계 복귀를 확인했다.
- 3단계는 단서 3개 확인 전 선택지 잠금, 오답→`3-1` IF→재시도→3단계 복귀를 확인했다.
- P1-02 계획의 1~3단계 정상·오답·IF 회귀 기준을 충족했다. 실제 학생 활동 시간 측정은 별도 검토로 남겼다.

## 2026-09-01 — P1-04 민감 역사 주제 자료 흐름 브라우저 확인

- `regular_japanese_rule_2` 1단계에서 정책 문서·이름 통제·학교생활 단서를, 2단계에서 동원 기록·피해자 증언·기억 활동 단서를 확인했다.
- 2단계의 안내가 피해 재현이 아닌 기록·증언·기억 활동 중심으로 표시되고, 기존 전투 그래픽 문구가 나타나지 않는 것을 확인했다.
- 2단계의 정상 선택 후 3단계로 진입했으며, 3단계에서 사전 원고·맞춤법·언어생활 자료 단서가 표시되는 것을 확인했다.
- 이는 현재 공통 evidence 장면의 자료 흐름 확인이며, Claude의 Phase 38 고유 장면 교체 후 최종 시각 검수는 별도로 남겼다.

## 2026-09-01 — P2-01 모달 접근성 브라우저 확인

- 로컬 Chrome에서 퀴즈와 도감 모달이 `role="dialog"`와 제목 연결(`aria-labelledby`)을 제공하는 것을 확인했다.
- 퀴즈 닫기 버튼(`골든벨 퀴즈 닫기`)과 도감 닫기 버튼(`역사 도감 닫기`)에 명시적 접근성 이름이 있고, 닫은 뒤 포털로 복귀했다.
- 점검 탭의 콘솔 오류는 0건이었다. 실제 키보드 포커스 순서와 보조기기 낭독 확인은 남겨 두었다.

## 2026-09-01 — Phase 38 Codex 승인 확인

- Claude의 `4a3c577` 구현 커밋과 `88ed08c` 핸드오프 문서를 검토했다.
- Phase 38 대상 JSON 5개에서 scene 값 15개만 교체되었고, `hotspots`·`interaction`·`completion.target`·단계 구조·학습 문구의 보호 필드 차이는 0건이었다.
- JSON의 Phase 38 scene 15개가 모두 `drawConfiguredSceneBackground`에 존재하며, 전체 고유 renderer scene 키 97개와 일치하는 것을 독립 검사했다.
- 표준 데이터·MUD 무결성·계약·정적 자산·카탈로그·출처·탭 감사·런타임 검증과 JS 문법 검사 3종, 커밋 diff 공백 검사를 모두 통과했다. 활동 시간 감사는 임시 복사본에서 `32개 MUD / 28개 Regular / 14개 보완 신호`로 확인했다.
- 캐시 갱신 URL로 실제 브라우저에서 `memorial-candlelight`를 확인했다. 촛불·꽃·기록 문서 중심 장면이며 legacy `호국 결전` 문구가 없고 콘솔 오류도 0건이었다.
- 구현 커밋과 핸드오프 문서는 내용상 승인한다. 다만 현재 Codex 문서 변경분과 untracked 핸드오프 지시서가 작업 트리에 있으므로, 이번 승인에서는 push하지 않고 사용자 승인 후 원격 반영한다.

## 2026-09-01 — P2-03 중복 차시 primary MUD 브라우저 확인

- 2단원 7차시 포털에서 `regular_joseon_diplomacy`만 표시되고 보조 `regular_myeongnyang`은 기본 목록에서 제외되는 것을 확인했다.
- 3단원 12차시 포털에서 `regular_post_war`만 표시되고 보조 `regular_korean_war`는 기본 목록에서 제외되는 것을 확인했다.
- 두 primary 버튼을 실제로 눌러 각 MUD 화면에 진입했으며, 점검 탭 콘솔 오류는 0건이었다.
- 별도 보조 MUD 노출과 태블릿 육안 검증은 후속 확인 대상으로 남겼다.

## 2026-09-01 — P2-01 키보드 모달 접근성 보강

- 퀴즈·도감 모달을 연 뒤 닫기 버튼으로 포커스를 이동시키고, Tab 키가 모달 내부 요소만 순환하도록 `app.js`에 공통 키보드 처리를 추가했다.
- Escape 키로 열린 모달을 닫고, 모달을 연 원래 버튼으로 포커스를 복귀하도록 했다.
- `index.html`의 `app.js`에도 캐시 버전 쿼리를 추가해 브라우저가 이전 접근성 코드를 계속 사용하는 문제를 방지했다.
- 로컬 Chrome에서 퀴즈 모달의 닫기 버튼 포커스, Tab 내부 이동, Escape 닫기, 트리거 복귀를 확인했으며 콘솔 오류는 0건이었다.
- 실제 보조기기 낭독과 색상 없이 상태를 구분하는 검증은 후속 확인 대상으로 남겼다.

## 2026-09-01 — P2-02 유물·보상 설명 2차 대조

- 감사 대상 `art_1`, `art_3`, `art_4`, `art_15`, `art_16`, `art_18`의 현재 설명을 계획서의 수정 방향과 대조했다.
- 과장·단정 표현은 완화된 상태이며, 유물 ID·이름·등급·보상 연결은 변경하지 않았다.
- 유물·보상 무결성, MUD 계약, 출처 검증을 재실행해 모두 통과했다.
- 추가 코드·데이터 수정은 하지 않았으며, 역사 사실에 대한 교사 최종 검토 대기 상태를 유지한다.
## 2026-09-01 — 사용자 1차 콘텐츠 승인 및 후속 검토 항목

- Phase 38 근현대 15개 장면은 역사 상황 표현이 대체로 교과서와 유사하고, 전체 그래픽 구성 방향이 적절하다는 사용자 검토를 받았다.
- 선택지는 정상 작동하는 것으로 확인했다. 모바일 화면 검토는 추후 진행한다.
- 민감한 역사 내용은 대체로 적절하다는 검토를 받았다.
- 그래픽은 학습 내용 확정 후 단계별로 미세 조정할 수 있도록 남겼다.
- 유물·보상 관련 이미지는 설명과 완전히 다른 사례가 많아 별도 정합성 검토 항목으로 등록했다.
## 2026-09-01 — 민감 역사 glossary 표현 완화

- 사용자 승인에 따라 `regular_japanese_rule_2`의 glossary 2건만 수정했다.
- 일본군 ‘위안부’ 정의는 사실을 유지하면서 “성적 착취를 한 중대한 인권 범죄”로 표현을 완화했다.
- 조선어학회 사건 정의는 사실을 유지하면서 “가두고 심하게 고문해 … 목숨을 잃은 사건”으로 표현을 완화했다.
- `instruction`·`infoText`·`feedback`·`narrative`·`completion`·`simulator`와 장면 렌더러는 변경하지 않았다.
## 2026-09-01 — IF 스테이지 6개 1차 내용 검토

- `regular_korean_war:3-1`, `regular_joseon_diplomacy:1-1/2-1/3-1`, `regular_independence_army:3-1`, `regular_goryeo_culture:1-1`의 현재 `narrative`와 재시도 선택지를 검토했다.
- 6개 모두 자료 재확인·비교로 복귀하며, 가상 실패 결과를 역사적 사실처럼 단정하거나 폭력·감정을 정답으로 강요하는 표현은 발견하지 못했다.
- 단계별 사용자 최종 확인 포인트는 `codex_if_stage_review.md`에 기록했다. 문구 수정은 사용자 승인 전까지 보류한다.
## 2026-09-01 — 유물 아이콘·보상 토스트 정합성 수정 확인

- Claude가 승인된 `art_3`, `art_6`, `art_10`, `art_11`, `art_12`, `art_13`, `art_deep_2`의 아이콘을 유물명에 맞게 교체했다.
- `js/mudEngine.js`의 MUD 완료 보상 토스트가 `artifactId` 대신 사람이 읽는 유물명을 표시하도록 수정했다.
- 커밋 `a2ad6eb`의 변경 범위와 Codex 재검증 결과를 확인했다. 게임 데이터·MUD 무결성·계약·정적 자산·카탈로그·출처·시뮬레이터 런타임·JavaScript 문법 검증이 모두 통과했다.
- `art_19`, `art_22` 등 정확한 한국식 성곽·궁궐 이모지가 없는 낮은 위험도 항목은 변경하지 않았다.

## 2026-09-01 — 접근성 상태 표현 코드 점검

- 선택지 잠금 상태가 네이티브 `disabled`와 `aria-disabled`로 함께 전달되고, 선택지에는 색상 외에 A/B/C 문자 표식이 있음을 확인했다.
- 시뮬레이터 피드백·진행률은 `role="status"`와 `aria-live`로 읽히도록 구성되어 있다.
- 실제 보조기기 낭독과 색상 없이 완료·비활성 상태를 구분하는 기기 검증은 사용자 환경 점검으로 남겼다.

## 2026-09-01 — 테마 피드백 배너 명암 대비 보강

- 어두운 MUD 테마에서 `mn-canvas-feedback`의 어두운 글자가 거의 보이지 않던 공통 대비 문제를 수정했다.
- 테마 배경색과 흰색·어두운 글자색의 대비를 계산해 더 높은 대비의 글자색을 선택하도록 했다.
- 기존 진행 상태·문구·시뮬레이터 구조는 변경하지 않았다.
- 375×812 모바일 Chrome에서 어두운 `regular_japanese_rule_1`과 밝은 `regular_paleolithic`을 확인했으며, 두 화면 모두 배경에 맞는 흰색 글자와 콘솔 오류 0건을 확인했다.

## 2026-09-01 — 반복 탭·활동 시간 감사 수치 구분 정정

- `scripts/09_audit_tap_resistance.py` 재실행 결과 반복 탭 구조 후보는 40개임을 확인했다.
- 활동 시간 감사의 Regular 보완 신호 16종과 반복 탭 구조 후보 40개는 서로 다른 지표이므로 분리해 기록했다.

## 2026-09-01 — 반복 탭 후보 40개 우선순위 분류

- 구조 감사 후보 40개를 scene 보유 36개와 Codex가 별도 확인할 scene 없는 4개로 분리했다.
- 우선 확인 대상은 `regular_gwangbok:3`, `regular_myeongnyang:4`, `regular_bronze_age:1/4`이며, 단일 버튼·특수 슬라이더를 일반 반복 탭 활동과 동일하게 일괄 수정하지 않도록 했다.
- 상세 판단 기준과 다음 조치는 `codex_tap_resistance_priority.md`에 기록했다.

## 2026-09-01 — 중간 점검 문서 동기화 및 후속 대기 항목 정리

- Claude의 IF 6개 최종 검토를 반영해 `regular_joseon_diplomacy:2-1`의 `척화·주화` 용어 glossary 추가 여부를 사용자 확인 대기로 남겼다.
- `regular_sejong`은 2단계와 3단계 simulator에 각각 중복 `instruction`·`feedback`이 있음을 확인했으며, 자료 중심의 앞 문구를 유지하고 뒤 문구를 삭제하는 안을 사용자 승인 대기로 남겼다.
- `BACKLOG.md`, `project_context.md`, IF·등록 경로·Phase 38 계획서의 완료 수치와 상태를 최신 커밋 기준으로 동기화했다.
- IF 6개 사용자 최종 승인, 세종 중복 키 결정, 실제 태블릿·보조기기·학생 활동 시간 측정은 후속 검토 항목으로 유지한다.

## 2026-09-01 — IF 승인 및 세종 simulator 중복 키 정리

- 사용자가 IF 6개를 현행 유지 승인했다. `regular_joseon_diplomacy:2-1`의 “척화·주화” glossary 보강은 별도 보류한다.
- `regular_sejong` 2·3단계 simulator의 뒤쪽 중복 `instruction`·`feedback` 4줄을 삭제하고, hotspot과 직접 연결되는 자료 중심 문구를 유지했다.
- `completion.target`, hotspots, `infoText`, 단계 구조와 선택지 흐름은 변경하지 않았다.

## 2026-09-01 — 학생 실제 활동 시간 관찰 기록

- 수업 종료 10분 전 1단원 2차시 `regular_paleolithic`과 3차시 `regular_neolithic`을 연속 제시했다.
- 학생들이 두 활동을 모두 4분 이내에 완료했으며, 기존 설계 추정치와 구분되는 실제 관찰값으로 `activity_duration_audit.md`에 기록했다.
- 빠른 완료 과정에서 학생들이 오타 또는 문맥 문제를 발견해 교사에게 제시했다.
- Regular를 일괄적으로 늘리기보다 빠른 학생용 선택형 유물 확장 활동을 검토 대상으로 남겼다.
- 표본 수와 단계별 소요 시간이 없어 P1-02 전체 완료로 처리하지 않았다.

## 2026-09-01 — 1단원 3차시 신석기 MUD 문맥 정합성 수정

- 학생 제보를 따라 `regular_neolithic` 2~3단계의 설명·선택지·시뮬레이터 연결을 대조했다.
- 2단계는 토기 제작, 3단계는 가락바퀴·뼈바늘을 사용하는 의생활 활동으로 구조가 정해져 있음을 확인했다.
- 2단계와 2-1의 농기구 관련 narrative를 토기 제작 문맥으로, 3단계와 3-1의 토기 관련 narrative를 의생활 문맥으로 최소 수정했다.
- 선택지의 `next`, simulator, completion, 보상과 단계 구조는 변경하지 않았다.
- JSON 데이터·MUD 무결성·계약·출처·시뮬레이터 런타임·`git diff --check` 검증을 통과했다.

## 2026-09-01 — 선택지 문장 길이·정답 단서 감사

- `scripts/12_audit_choice_bias.py`를 추가해 다중 선택 결정 단계 124개를 감사했다.
- 정답 위치는 124개 모두 원본 배열 1번이었으나, 런타임 표시 무작위화가 이미 적용되어 위치 단서는 실행 중 완화된다.
- 정답 문장이 오답 평균보다 10자 이상 긴 단계 40개, 모든 오답보다 긴 단계 115개를 확인했다.
- 학생의 “긴 문장만 고르면 정답” 반응과 일치하는 정량 신호로 판단하고, 상위 단계부터 교사 검토 후 문장 보정을 진행하기로 했다.
- 이번 작업에서는 JSON 선택지 문구를 일괄 수정하지 않았다.

## 2026-09-01 — 선택지 품질 보정 1차 및 브라우저 회귀

- `regular_gwangbok:1`, `regular_myeongnyang:1`, `regular_goryeo_society:1`의 선택지 문장을 질문과 연결되도록 보정했다.
- `correct`, `next`, `sound`, 시뮬레이터와 보상 구조는 변경하지 않았다.
- 선택지 감사에서 정답이 오답 평균보다 10자 이상 긴 단계가 40개에서 34개로, 모든 오답보다 긴 단계가 115개에서 114개로 감소했다.
- Chrome에서 `regular_gwangbok:1`의 시뮬레이터 완료 후 정상 선택→2단계 진입과 2단계 오답→`2-1` IF 이동을 확인했으며 오류·경고 로그는 없었다.
- 회귀 중 2단계 질문이 5·10 총선거인데 선택지는 건국준비위원회이고, `2-1`은 선거권을 설명하는 별도 문맥 불일치를 발견해 BACKLOG로 분리했다.

## 2026-09-01 — 광복 MUD 2단계 문맥 정합성 수정

- `regular_gwangbok:2`의 건국준비위원회 활동과 어긋나던 총선거 설명을 광복 직후 치안 유지·건국 준비 설명으로 수정했다.
- `regular_gwangbok:2-1`을 불평등 선거권 설명에서 치안 공백과 건국준비위원회 재확인 설명으로 수정했다.
- 2단계 선택지의 문장 길이와 의미 형식을 함께 균형화했으며, 3단계 총선거 흐름은 유지했다.
- Chrome에서 1단계 시뮬레이터 완료→정상 선택→2단계 진입을 확인했고, 수정된 2단계 문구와 선택지 표시를 확인했다.
- 브라우저 오류·경고 0건, JSON·MUD 무결성·계약·출처·시뮬레이터 런타임·선택지 감사·`git diff --check`를 통과했다.

## 2026-09-01 — 고려·조선 선택지 문맥 정합성 수정

- `regular_goryeo_society:3`의 제사 설명을 부석사 무량수전 배흘림기둥 탐구로 정렬하고, `3-1` IF도 배흘림기둥 원리 재확인으로 맞췄다.
- `regular_joseon_founding:2`의 경복궁 이름 설명을 한양 4대문과 인의예지 탐구로 정렬하고, `2-1` IF도 성문 이름의 유교 덕목 재확인으로 맞췄다.
- 선택지의 `correct`, `next`, `sound`, 시뮬레이터, 보상과 단계 구조는 변경하지 않았다.
- JSON·MUD 무결성·계약·출처·선택지 편향·시뮬레이터 런타임·JavaScript 문법·`git diff --check` 검증을 통과했다.

## 2026-09-01 — 학생 피드백 기반 기획 기준선 갱신

- `PRD.md`, `TECH_STACK.md`, `DECISIONS.md`, `BACKLOG.md`, `agents.md`, `README.md`, `project_context.md`에 실제 교실 기반 반복 개선 사이클을 반영했다.
- 4분 이내 완료 관찰을 선택지 난이도·시뮬레이터 완료 조건·읽기 속도·교실 내 정보 공유로 분리해 진단하도록 정리했다.
- 최소 플레이 로그는 개인정보 없이 브라우저 내부에서 검토하고, 유물은 수집 보상에서 관찰·비교·주장·근거 추론 자원으로 확장하는 방향을 기록했다.
- 친구 비교·협력과 역사 타이쿤은 서버 없는 작은 실험 이후로, 백엔드·DB·로그인·실시간 멀티플레이·공개 순위표는 `Future / Not Now`로 경계를 명시했다.
- 선택지 편향 최신 감사 수치(124개 결정 단계, 34개·114개)를 `BACKLOG.md`와 `docs/audits/choice_bias_audit.md`에 동기화했다.
- 검증: `python scripts/12_audit_choice_bias.py`, `git diff --check` 통과.

## 2026-09-01 — 게임성·Vercel·Supabase 점진 도입 기준선 갱신

- `PRD.md`와 `USER_FLOWS.md`에 탐험·발견·자료 추론·유물 활용·근거 공유를 연결하는 장기 플레이 루프를 추가했다.
- 빠른 완료 문제는 필수 경로를 무조건 늘리지 않고 선택 탐험과 추가 역사 단서로 확장하는 방향을 기록했다.
- `TECH_STACK.md`, `DECISIONS.md`, `project_context.md`, `README.md`, `BACKLOG.md`, `agents.md`에서 현재 GitHub Pages 운영과 목표 Vercel 배포를 구분했다.
- Supabase는 `play_events` 익명 로그부터 검토하고, localStorage는 즉시 상태·복원·네트워크 장애 대응으로 유지하도록 정리했다.
- 개인정보 최소화, RLS, secret/service-role 키 비노출, Anonymous Auth·Realtime·역사 타이쿤의 후순위 게이트를 문서화했다.
- 이번 단계에서는 Supabase 프로젝트·테이블·Vercel 설정·Auth·Realtime·플레이 로그 코드를 구현하지 않았다.

## 2026-09-01 — 중앙 INBOX 및 문서 승격 workflow 도입

- 사용자가 아이디어·수업 관찰·기술 검토를 `INBOX.md` 한 곳에 기록하도록 중앙 입력 창구를 만들었다.
- `EXPERIMENTS.md`, `ROADMAP.md`, `ARCHITECTURE.md`를 추가해 수업 실험, 구현 순서, 시스템 연결 구조를 분리했다.
- `agents.md`, `README.md`, `project_context.md`에 INBOX 원문 보존과 역할별 문서 승격 규칙을 연결했다.
- 판단이 어려운 내용은 확정하지 않고 INBOX에 보존하며, 기존 문서와 충돌하면 사용자에게 보고하도록 규칙화했다.
- 현재 저장소의 루트 문서 링크를 보존하기 위해 `docs/`로 파일을 이동하지 않았다.

## 2026-09-01 — 다중 에이전트 작업 규칙 명시

- `agents.md`에 Codex CLI와 Claude Code의 동일 저장소 작업 원칙을 추가했다.
- 동일 파일 동시 수정 금지, 구현 에이전트와 검토 에이전트 분리, 병렬 작업 시 branch/worktree 우선 원칙을 명시했다.

## 2026-09-01 — 문서 상태 대조 1차 정리

- `BACKLOG.md`에서 이미 해결된 `regular_gwangbok` 문맥 정합성 항목을 미완료 후속 목록에서 제거했다.
- IF 스테이지 6개는 사용자 최종 승인 완료 상태이므로 `사용자 승인 대기` 표현을 정정했다.
- 구현 계획서 파일 자체의 삭제·이동·병합은 수행하지 않고, 상태가 불명확한 문서는 보류했다.

## 2026-09-01 — Claude 문서 정리 감사 반영

- Claude의 `document_cleanup_priority_audit.md`를 인수하고, 실제 `origin/main` 이력과 대조했다.
- 문서 재배치 계획서와 재배치·Phase 37·Phase 38 핸드오프의 오래된 push 보류 상태를 현재 원격 반영 상태로 정정했다.
- 반복 탭 내성 계획서의 실제 반영 수치를 5개 MUD로 통일했다.
- 유물 복원 퍼즐 계획은 코드 미구현이므로 활성 작업으로 유지했으며, 문서 삭제·이동·병합은 수행하지 않았다.

## 2026-09-01 — 선택지 무작위화 계획 상태 정정

- `implementation_plan_choice_randomization.md` 상단에 2026-08-26 구현·검증 완료 상태를 명시했다.
- `js/mudEngine.js`의 선택지 복사본 Fisher–Yates 셔플과 기존 분기 보존 결과를 계획서와 대조했다.
- 선택지 길이·구체성 편향은 무작위화와 다른 문제이므로 `implementation_plan_choice_quality_batch.md`와 별도 감사 문서에서 계속 관리한다.

## 2026-09-01 — 과거 플레이타임 확장 계획 상태 정정

- `implementation_plan_mud_depth_expansion.md` 상단에 `superseded` 상태를 명시했다.
- 과거 계획의 전면적 장면 확장·자원값·분기 구조는 현재 실행 기준으로 사용하지 않도록 하고, 탐험·발견의 최소 실험은 `EXPERIMENTS.md`와 `ROADMAP.md`로 연결했다.
- 과거의 정량 진단과 설계 근거는 삭제하지 않고 기록으로 보존했다.

## 2026-09-01 — 삼국 심화 인터랙티브 화면 내용 정합성 개선

- `deep_three_kingdoms`의 황산벌, 매소성, 기벌포, 천문령 장면을 사건별 화면으로 분리했다.
- 매소성은 산성·장창 방진·보급로를, 기벌포는 금강 하구 갯벌·당나라 수군·신라 대장선을 보여 주도록 구성했다.
- 삼국 심화의 문서형 시뮬레이터에는 남북국·삼국 통일 전쟁의 단계별 제목과 단서를 표시하도록 했다.
- 기존 명량대첩 화면과 대한국새 화면이 삼국 심화에 재사용되던 문제를 전용 렌더러 분기로 차단했다.
- `node --check`, MUD 계약 검사, 시뮬레이터 런타임 검사를 통과했고 브라우저에서 황산벌부터 기벌포까지 진행을 확인했다.

## 2026-09-02 — Track A 런타임 안정화

- 시뮬레이터가 없는 IF 단계에서도 이전 활동 상태가 남지 않도록 `renderStage()`와 `setupSimulator()`의 초기화 경계를 정리했다.
- 시뮬레이터 없는 단계에서는 인터랙티브 카드·위젯·대체 조작을 숨기고, 다음 시뮬레이터 단계에서는 카드와 진행 상태를 복원하도록 했다.
- `gwangbok-flag`·`gwangbok-vote` 캔버스 직접 조작도 대체 버튼과 동일하게 `simulatorProgress`와 완료 판정을 갱신하도록 수정했다.
- 스테이지 전환 시 `simActionIds`를 초기화하고, 해당 상태·캔버스 진행·IF 카드 숨김 회귀 테스트를 `scripts/05_test_simulator_runtime.js`에 추가했다.
- 브라우저에서 `regular_balhae` 정상 완료 → 오답 `1-1` IF → 재시도 흐름을 확인했다. 카드가 `display: none`/`block`으로 올바르게 전환되고 재시도 시 게이지가 0%로 초기화되었으며 콘솔 오류·경고는 0건이었다.
- 검증: `node scripts/05_test_simulator_runtime.js`, 전체 JSON·MUD 계약·자산·카탈로그·출처·탭 내성·선택지 감사, `node --check` 4종, `git diff --check` 통과.
