# 구현 계획 — 선택지 문장 품질·정답 단서 보정 1차 묶음

## 상태

- 상태: in-progress — 1차 문구 수정·대표 브라우저 일부 회귀 완료, 추가 문맥 오류 점검 대기
- 우선순위: P1
- 근거: 학생 현장 반응 “긴 문장만 고르면 정답”, 전체 선택지 단서 감사
- 감사 결과: 다중 선택 단계 124개 중 정답이 오답 평균보다 10자 이상 긴 단계 40개, 모든 오답보다 긴 단계 115개

## 목적

학생이 선택지의 길이·구체성·긍정적인 어조만으로 정답을 추측하지 않고, 화면의 자료와 역사적 맥락을 근거로 판단하게 한다. 문장을 억지로 어렵게 만들거나 모든 선택지를 길게 늘리는 것이 아니라, 질문·선택지·시뮬레이터 활동의 의미를 먼저 맞춘다.

## 1차 대상

학생에게 노출되는 Regular MUD 중 감사 차이가 크고 의미 정합성 확인이 필요한 세 단계로 제한한다.

| 단계 | 감사 신호 | 1차 확인 방향 |
|---|---|---|
| `regular_gwangbok:1` | 정답 44자, 오답 평균 16.5자 | 광복의 상징과 태극기 활동을 유지하면서 선택지 형식·길이 균형화 |
| `regular_myeongnyang:1` | 정답 35자, 오답 평균 18.5자 | 수군 유지 판단의 근거와 선택지 의미를 일치시키고 인용문 과도한 단서 완화 |
| `regular_goryeo_society:1` | 정답 33자, 오답 평균 19자; 질문과 정답 의미 불일치 | 고려 상속 문제에 맞는 선택지로 교정하고 홍패 관련 오연결 여부 확인 |

`deep_modern`과 나머지 Regular 단계는 이번 묶음에서 문구를 수정하지 않는다.

## 현재 실행 결과

- `regular_gwangbok:1`, `regular_myeongnyang:1`, `regular_goryeo_society:1`의 선택지 문구를 보정했다.
- 후속 대조에서 확인된 `regular_goryeo_society:3`, `regular_joseon_founding:2`의 narrative·IF 문맥도 각각 부석사 배흘림기둥, 한양 4대문 활동에 맞게 정렬했다.
- `regular_gwangbok:2`의 narrative·IF 문맥과 선택지 길이도 건국준비위원회 활동에 맞게 정렬했다.
- 정적 검증과 선택지 감사는 통과했다. 1차 보정 및 광복·고려·조선 대상 단계의 선택지 균형화 후 정답이 오답 평균보다 10자 이상 긴 단계는 34개로 집계되었다.
- 대표 브라우저에서는 `regular_gwangbok:1`의 정상 진행, 2단계 진입, 2단계 오답→`2-1` IF 이동을 확인했다.
- 브라우저 확인 중 `regular_gwangbok:2`와 `2-1`의 문맥 불일치를 발견해 별도 BACKLOG 항목으로 분리했다.

## 검토·수정 원칙

1. 질문이 묻는 역사적 문제와 정답 선택지의 행위가 직접 연결되어야 한다.
2. 정답만 길거나 설명적이지 않도록 세 선택지의 문장 형식과 정보량을 맞춘다.
3. 오답은 무의미한 농담보다 자료에 비추어 판단 가능한 대안으로 작성한다.
4. 핵심 교과 개념과 유물명은 삭제하지 않는다.
5. 선택지 텍스트의 `correct`, `next`, `sound`와 시뮬레이터·IF 연결은 보존한다.
6. 정답 위치는 원본 JSON에서 재배열하지 않고 런타임 무작위화에 맡긴다.
7. 역사적 사실을 새로 추가하거나 기존 출처 범위를 벗어나는 문장은 교사 검토 대상으로 보류한다.

## 예상 변경 범위

- `data/mud/regular_gwangbok.json`: `1`단계 선택지 텍스트만 검토·수정
- `data/mud/regular_myeongnyang.json`: `1`단계 선택지 텍스트만 검토·수정
- `data/mud/regular_goryeo_society.json`: `1`단계 narrative·선택지·관련 IF 연결을 대조하고 필요한 최소 필드만 수정
- `choice_bias_audit.md`: 수정 후 수치 갱신
- `walkthrough.md`: 변경·검증 결과 기록

변경하지 않는 범위:

- 선택지 표시 로직과 무작위화 코드
- 시뮬레이터 장면·완료 조건
- 보상 ID와 도감 데이터
- Deep-dive 전체
- 3개 MUD 외 선택지 문장 일괄 수정

## 완료 기준

- [ ] 세 단계의 질문·정답·오답이 의미상 연결된다.
- [ ] 정답만 지나치게 길거나 구체적인 표현이 되지 않는다.
- [ ] 기존 `next` 분기와 IF 재시도 흐름이 유지된다.
- [ ] JSON·MUD 무결성·계약·출처 검증을 통과한다.
- [ ] 선택지 길이 감사 수치가 갱신된다.
- [ ] 대표 브라우저에서 정상·오답·IF 회귀를 확인한다.

## 승인 후 실행 순서

1. 세 단계의 현재 질문과 출처를 기준으로 문장 수정안을 작성한다.
2. 문장 수정 후 선택지·분기 필드를 비교해 보호한다.
3. 전체 정적 검증과 선택지 편향 감사를 실행한다.
4. 대표 브라우저에서 선택지 표시·정상 진행·오답 복귀를 확인한다.
5. 학생에게 긴 문장 단서가 사라졌는지 재확인한다.

## Claude 검토 (2026-09-01)

사용자에게 3단계를 각각 보여주고 승인 여부를 확인함.

- **`regular_gwangbok:1`, `regular_myeongnyang:1`: 승인.** 서사-정답 의미는 원래도 정확했고, 이번 수정으로 오답에 근거 어휘가 보강되어 길이 격차만 줄었다. `next`/`correct`/`sound` 불변 확인.
- **`regular_goryeo_society:1`: 이번 문구 수정만으로는 완결되지 않음 — 별도 확인 필요.**
  - 이번 커밋 전(HEAD) 상태를 보면 이 단계는 애초에 두 겹으로 어긋나 있었다: `narrative`는 손변의 남매 상속 재판 이야기인데, `choices`는 "홍패를 받아 관직에 나아간다" 같은 과거제 선택지였다(서사-선택지 불일치). 이번 수정은 `choices`를 `narrative`(상속 재판)에 맞춰 교정해 그 불일치는 해결했다.
  - 그런데 `character`("과거 응시생"), `location`("고려 개경 과거 시험장"), `glossary`("홍패"), `badge`("능력 중심의 과거제"), `simulator.infoText`("광종은 노비안검법과 과거제를…"), `simulator.instruction`("고려 과거 시험과 홍패 확인"), 로드맵 라벨(`"1. 광종의 과거제(홍패)"`)은 전혀 수정되지 않아 여전히 과거제/홍패 주제다. 지금은 "상속 재판 서사 + 과거제 껍데기"가 섞여 있다.
  - `git show b0f2f96:data/mud/regular_goryeo_society.json`(Phase 1 리팩터링 시점 원본)을 보면 이 스테이지는 원래 순수 과거제/홍패 내용이었다(narrative·choices 모두). 이 MUD의 보상 유물도 `art_10 "고려 과거 합격증 '홍패'"`이고 엔딩 단계도 "국보 도감에서 홍패를 확인하세요"라고 안내하므로, 홍패/과거제는 이 MUD 전체의 핵심 학습 주제로 보인다.
  - **결정 필요**: (a) 과거제/홍패 주제를 스테이지 1에서 완전히 걷어내고 `character`/`location`/`glossary`/`badge`/`simulator`/로드맵 라벨을 상속 재판에 맞게 다시 쓴다 — 이 경우 홍패 관련 보상·엔딩 문구도 함께 바꿔야 한다. (b) 과거제/홍패 내용을 별도 스테이지로 복원하고, 지금의 상속 재판 도입부를 그 뒤(또는 스테이지 2 앞)로 옮긴다 — 이 경우 로드맵 재배치가 필요하다.
  - 이 판단은 선택지 문장 균형화 범위를 넘는 콘텐츠 구조 결정이라 사용자 승인 없이 Sonnet이 임의로 고치지 않았다. Codex가 이어서 검토해 주면 좋겠다.
