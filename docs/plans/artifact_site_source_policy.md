# 유물·유적 출처 확인 규칙 (대조실 트랙)

- 작성: Sonnet 5, 2026-09-10 (트랙 2 지시서 `docs/handoff/claude_track2_artifact_site_instruction.md` P0)
- 적용 범위: `data/artifactComparisons.json`의 모든 신규·기존 유물(`artifactA`/`artifactB`)·유적(`site`) 항목
- 이 활동은 실물 사진을 앱에 직접 싣지 않는다(이모지+텍스트, 바깥 링크만 제공). 따라서 **이미지 이용조건(공공누리 유형·변경금지 여부)은 기능상 영향이 없다** — 기록은 남기되 이 규칙 때문에 데이터 추가가 막히지는 않는다.

## 1. 유물(움직일 수 있는 개별 소장품) — museum.go.kr 기준

확인해야 할 것, 전부 채워야 `sourceNote`에 "확인함"으로 적는다:

| 항목 | 필드 | 확인 방법 |
|---|---|---|
| 국립중앙박물관 소장품번호 | `museumId` | museum.go.kr 소장품 검색에서 실물 페이지 열어 직접 확인 |
| `relicId` | `museumUrl`의 쿼리스트링 | 소장품 검색 상세 URL(`/site/main/relic/search/view?relicId=`)에서 확인. **전시실 안내 페이지(`/MUSEUM/contents/M....do?...relicId=`)는 다른 번호 체계다** — 헷갈리지 않는다(2026-09-08에 실제로 헷갈려 엉뚱한 유물이 뜬 적 있음) |
| 공공누리 이용조건 유형 | `license` | 소장품 상세 페이지의 "공공누리" 표시 확인. 없으면 "확인하지 못함"이라고 명시하고 추측하지 않는다 |
| 출토지 | `location` | 게임 속 다른 데이터(예: art_1의 findspot)와 일치하는지 대조한다 |

## 2. 유적(장소, 소장품번호가 없는 대상) — 국가유산포털(heritage.go.kr) 기준

유적은 "소장품"이 아니라 "국가유산(사적 등 지정문화재)"이므로 위 1번 기준을 그대로 쓸 수 없다. 대신:

| 항목 | 필드 | 확인 방법 |
|---|---|---|
| 지정 종별 | `site.kind` | "사적", "천연기념물" 등 — heritage.go.kr 상세 페이지 상단에 명시됨 |
| `ccbaCpno` | `site.url`의 쿼리스트링 | `culSelectDetail.do?ccbaCpno=...` 형태. 검색 결과 링크를 그대로 따라가 실제 페이지가 뜨는지 직접 확인한다(제목이 원하는 유적과 일치하는지) |
| 사진 유무 | `site.hasPhoto` | 상세 페이지에 대표 이미지(전경·야경 등)가 실제로 있는지 WebFetch/직접 열람으로 확인. 없으면 링크를 걸지 않는다 |
| 확인 날짜 | `site.checkedAt` | 링크를 실제로 열어 확인한 날짜(ISO, `YYYY-MM-DD`) |
| 지정 면적·연도 | (선택, `site.note`에 문장으로) | 상세 페이지의 "지정일"·"면적" — 있으면 규모 비교에 쓸 수 있어 기록해 둔다 |

## 3. 공통 규칙

1. **확인하지 못한 항목을 추측으로 채우지 않는다.** museum.go.kr·heritage.go.kr 검색 UI가 동적이라 relicId/ccbaCpno를 못 찾을 때가 있다 — 이럴 때는 그 페어(또는 그 항목)를 보류하고, `sourceNote`에 "확인하지 못함"이라고 정직하게 남긴다. 지금까지 실제로 보류한 사례: 고려 홍패(개인·문중 소장만 확인됨), 신라 금관과 짝지을 가야 금동관의 개별 소장품번호(상설전시 안내 페이지 설명만 확보).
2. **의심스러우면 사용자에게 물어본다.** 특히 유적이 5학년 수준에 적절한지(폭력·죽음 관련 유적 등)는 `TEACHING_artifact_comparison.md`의 기존 콘텐츠 점검 체크리스트를 그대로 적용한다.
3. **findspot 일치 확인.** 유물의 `location`(또는 `traits.location`)과 유적의 `site.name`이 실제로 같은 장소를 가리키는지 대조한다(예: "충남 부여 송국리" ↔ "부여 송국리 유적"). 다른 이름이면 별도 확인 없이 연결하지 않는다.
4. **`checkedAt` 없는 site 필드는 만들지 않는다.** 링크를 걸기 전에 반드시 heritage.go.kr 페이지를 직접 열어(WebFetch 또는 직접 열람) 제목·설명·사진 유무를 확인한 뒤에만 데이터에 추가한다.
5. 이 문서는 이후 모든 신규 유물·유적 데이터가 따라야 하는 규칙이다 — 새 페어를 만들 때 이 문서의 표를 그대로 체크리스트로 쓴다.

## 4. 2026-09-10 기준 이미 확인된 유적(재사용 가능)

`data/artifactComparisons.json`에 이미 `site` 필드로 들어가 있고, 확인 과정도 `BACKLOG.md`(2026-09-10 항목)에 기록되어 있다. 새 페어를 만들 때 아래는 다시 조사할 필요 없이 그대로 재사용한다.

| 유적 | `ccbaCpno` | 관련 유물 |
|---|---|---|
| 서울 암사동 유적 | 1331102670000 | 빗살무늬 토기 |
| 부여 송국리 유적 | 1333402490000 | 민무늬 토기 항아리, 비파형(요령식) 동검 |
| 연천 전곡리 유적 | 1333102680000 | 주먹도끼 |
| 경주 대릉원 일원(금관총 포함) | 1333705120000 | 신라 금관(금관총 금관) |
| 고령 지산동 고분군 | 1333700790000 | 가야 금동관 |
| 공주 무령왕릉과 왕릉원 | 1333400130000 | 무령왕 금제 관식 |
| 고창 죽림리 지석묘군(고인돌, 세계유산) | 1333503910000 | (아직 페어에 미사용 — `art_3` 고창 탁자식 고인돌과 연결 가능) |

출토지를 모르는 전세품 유물(청자 상감 매병, 백자 달항아리, 분청사기 매병, 김홍도/신윤복 풍속화, 앙부일구, 자격루)은 `site` 필드를 일부러 비워 둔다 — 이 자체가 "모든 유물이 발굴된 것은 아니다"라는 정직한 교육 포인트다.
