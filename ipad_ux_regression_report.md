# iPad 시뮬레이터 UX 회귀 점검 보고서

점검일: 2026-08-27  
대상 화면: iPad 10세대 세로 기준 820×1180 CSS px  
방법: 로컬 배포본을 브라우저에서 실제 터치 대체 조작으로 점검

## 통과 항목

| 대상 | 확인 내용 | 결과 |
| --- | --- | --- |
| 구석기 1단계 | 세 자연환경 단서 확인 전 선택지 잠금, 완료 후 해제 | 통과 |
| 구석기 2단계 | 잘못된 순서 선택 뒤에도 재료가 잠기지 않고 재시도 가능 | 통과 |
| 고조선 1단계 | 지도 단서 3개와 선택지 잠금 상태가 정상 표시 | 통과 |
| 공통 핫스팟 대체 조작 | 버튼 최소 높이 44px 적용 | 통과 |
| 브라우저 콘솔 | 점검 흐름 중 오류 | 0건 |

## 수정 사항

- 구석기 1단계는 완료 조건이 세 장소 탐색인데 초기 안내가 두 장소만 언급하고 있었다. 안내를 `바위 그늘·강가·트인 평야를 모두 확인하세요!`로 수정했다.
- 캔버스 대신 사용할 수 있는 공통 핫스팟 버튼의 실측 높이가 35px였다. `min-height: 44px`를 공통 렌더러에 적용했다.
- 정적 배포 환경에서 이전 공통 스크립트가 남지 않도록 `mudSimulators.js`의 버전 쿼리도 갱신했다.

## 장면 맥락 보완 (2026-08-27)

- 구석기 공통 활동에 단색 배경 대신 활동별 벡터 장면을 적용했다: 동굴의 불씨, 한탄강 자갈밭의 주먹도끼, 평원의 동물 흔적과 바위 길목, 공동체 야영지, 해 질 녘 성찰 장면이다.
- 장면은 외부 이미지 파일 없이 캔버스로 그려 배포 용량·저작권·네트워크 환경의 영향을 받지 않으며, 단서 원형 버튼과 대체 조작을 그대로 유지한다.

## 신석기·고조선 확장 (2026-08-27)

- `scene` 키를 MUD JSON에 선언하고 공통 핫스팟 렌더러가 장면을 선택하도록 확장했다. 모드 이름만으로 시대별 화면을 추측하던 구조를 없앴다.
- 신석기는 한강변 마을, 빗살무늬 토기 제작, 가락바퀴·뼈바늘, 정착 생활 종합 장면을 적용했다.
- 고조선은 성장 지역 지도, 8조법 기록, 비파형 동검·고인돌, 건국 전승과 유물 종합 장면을 적용했다.
- iPad 10세대 세로 화면에서 두 시나리오의 단서 위치, 44px 대체 조작, 선택 잠금과 콘솔 오류 0건을 확인했다.

## 삼국·통일신라·발해 확장 (2026-08-27, Codex 브라우저 점검 대상)

- `scene` 키를 필수 핫스팟 활동 8곳에 추가로 선언했다: 삼국(백제 한강·서해·영토, 고구려 평양성·국내성·남진로, 신라 순수비·한강·당항성), 통일신라(9주 5소경 행정 기록, 불국사 석가탑·다보탑·청운교백운교, 석굴암 돔·배치·본존불, 통치·문화·교류 자료 종합), 발해(온돌·무덤·국서·주민 구성 자료 종합).
- `required: true`이지만 `hotspots`가 없는 게이지형 활동(삼국 4단계, 발해 1~3단계)은 공통 렌더러가 `scene`을 그리지 않으므로 이번 범위에서 제외했다. 레거시 단색·게이지 화면으로 남아 있다.
- 이번 변경은 코드 실행(`node --check`, 캔버스 렌더러 스모크 테스트: 8개 `scene` 모두 예외 없이 배경을 그리고 `setLineDash`를 원상 복구함, JSON-JS `scene` 문자열 일치)까지만 자체 확인했고, 실제 브라우저 시각·터치 점검은 수행하지 않았다.
- **Codex가 iPad 10세대 세로 화면(820×1180)에서 아래 8개 활동을 확인해야 한다**: 삼국(1·2·3단계), 통일신라(1·2·3·4단계), 발해(4단계).
  - 확인 기준: ① 단서 원 3개와 라벨이 배경 도형에 가려지지 않고 식별 가능 ② "진행: n/3" 텍스트가 배경과 충분한 대비를 가짐 ③ 대체 조작 버튼 최소 높이 44px 유지 ④ 단서 3개 확인 후 선택지 잠금 해제 정상 동작 ⑤ 화면 요소가 세로 뷰포트에서 잘리지 않음 ⑥ 브라우저 콘솔 오류 0건.

## deep-dive mode 오배정 수정 (2026-08-27, Codex 브라우저 점검 대상)

- `deep_joseon`(2·7), `deep_modern`(1·3·5), `deep_three_kingdoms`(1·7), `deep_prehistoric`(1a) 8개 선택 활동이 `mode`만 `culture-touch`/`battle-gauge`로 교체됐다. 이번 변경은 시각 장면이 아니라 활동 반응 여부(게이지 증가)를 되살리는 최소 수정이다.
- Node 스텁으로 `gaugeProgress` 증가만 확인했고, 실제 화면·터치 반응은 브라우저로 확인하지 않았다.
- **Codex가 확인해야 할 것**: 위 8개 활동을 열어 ① 화면이 더 이상 비어 있지 않고 제네릭 화면(사료 두루마리/붉은 원/초록 원 중 하나)이라도 보이는지 ② 화면 터치·대체 조작 버튼 클릭 시 게이지가 실제로 올라가는지 ③ 콘솔 오류 0건. 8개 모두 선택 활동이라 완료하지 않아도 다음 단계로 넘어갈 수 있다.

## Phase 34A 자료 종합형 11종 확장 (2026-08-27, Codex 브라우저 점검 대상)

- `regular_goryeo_culture`(4), `regular_goryeo_war`(4), `regular_independence`(4), `regular_japanese_rule_1`(4), `regular_japanese_rule_2`(4), `regular_joseon_folk`(4), `regular_joseon_founding`(4), `regular_joseon_status`(4), `regular_modern_open`(4), `regular_post_war`(4), `regular_sejong`(4) — 11개 MUD의 4단계 "자료 종합" 활동에 `scene`을 추가했다. 전부 핫스팟 좌표는 그대로다.
- 이번 변경은 캔버스 스모크 테스트(예외 0건, `setLineDash` 복원, JSON-JS scene 27개 전체 일치)까지만 확인했고, 실제 브라우저 시각·터치 점검은 하지 않았다.
- **Codex가 iPad 10세대 세로 화면(820×1180)에서 확인해야 할 것**: 위 11개 활동 각각 ① 카드 3장과 그 안 도형이 핫스팟 원·라벨을 가리지 않는지 ② "진행: n/3" 텍스트가 카드 바깥의 밝은 하단 배경과 충분한 대비를 갖는지 ③ 일제강점기 1·2 장면에서 사람·폭력 묘사가 없는지(설계상 없음, 육안 확인 필요) ④ 대체 조작 버튼 44px ⑤ 선택 잠금 해제 정상 동작 ⑥ 콘솔 오류 0건.

## Phase 34B 서사형 3종 확장 (2026-08-27, Codex 브라우저 점검 대상 — 민감도 높음)

- `regular_independence_army`(1~4), `regular_joseon_diplomacy`(1~4), `regular_korean_war`(1~4) 12개 단계에 `scene`을 추가했다. 핫스팟 좌표·완료 조건·피드백 문구는 그대로다.
- 이번 변경은 캔버스 스모크 테스트(예외 0건, `setLineDash` 복원, JSON-JS scene 39개 전체 일치)까지만 확인했고, 실제 브라우저 시각·터치 점검은 하지 않았다.
- **Codex가 반드시 육안으로 확인해야 할 것(민감 시대 표현 원칙 준수 여부)**:
  1. `righteous-army-flag`(단지동맹 태극기) — 신체 훼손·혈흔이 조금이라도 연상되지 않는지, 유물 보존실 구성(작업대·조명·확대경·장갑)으로만 보이는지.
  2. `korean-war-outbreak`·`korean-war-armistice` — 남북이 같은 회색으로 그려졌는지, 어느 한쪽도 적대색(빨강 등)으로 강조되지 않았는지, 협상 탁자가 좌우 대칭으로 보여 승패를 암시하지 않는지.
  3. `joseon-namhansanseong`·`korean-war-front` — 병사·무기·전투 장면이 없는지, 사람의 고통이 직접 묘사되지 않고 눈 결정·빈 항아리·짐 보따리 등 상징으로만 표현됐는지.
  4. 위 3항목 외 일반 확인: 카드/도형이 핫스팟 원·라벨을 가리지 않는지, "진행: n/3" 텍스트 대비(특히 `joseon-namhansanseong`은 배경이 어두운 편이라 우선 확인), 대체 조작 44px, 선택 잠금 해제, 콘솔 오류 0건.

## Phase 35 렌더러 확장과 고대 잔여 8종 (2026-08-27, Codex 브라우저 점검 대상)

- `regular_three_kingdoms`(4), `regular_three_kingdoms_life`(1~4), `regular_balhae`(1~3) 8개 게이지형 필수 활동에 `scene`을 추가했다. 이 활동들은 핫스팟이 없어 `mn-hotspot-actions` 대체 버튼이 원래도 표시되지 않는다(대체 조작은 게이지 탭 자체가 대신한다).
- `drawSim()`의 라우팅 분기가 바뀌었으므로, 이번 회귀 검증은 캔버스 스모크 테스트보다 한 단계 더 나아가 실제 `drawSim()`을 호출하는 Node 테스트로 **`scene` 미선언 5개 대표 레거시 mode가 완전히 그대로인지**(placeholder 문구 동일, 새 함수 미호출)까지 확인했다. 실제 브라우저 시각 점검은 하지 않았다.
- **Codex가 확인해야 할 것**:
  1. 이번 8개 활동이 실제로 새 장면으로 바뀌어 보이는지(제네릭 화면이 아닌지).
  2. **회귀 확인이 가장 중요**: 이번 Phase에서 건드리지 않은 기존 게이지형 활동들(예: 조선 경제, 태극기, 고인돌, 명량, 그리고 이번에 다루지 않은 나머지 `text-reading`/`battle-gauge`/`culture-touch` 활동들)이 이전과 똑같이 보이는지 — 특히 고인돌·태극기·명량은 전용 정밀 렌더러이므로 이번 변경의 영향을 받지 않아야 한다.
  3. 가야 철제 판갑옷·발해 건국 장면에 병사·무기·전투 묘사가 없는지.
  4. "진행: n/3" 대신 게이지 진행률 표시(`widget-gauge`)가 정상 작동하는지 — 게이지형은 캔버스 진행 텍스트가 없다.
  5. 콘솔 오류 0건.

## 후속 점검 기준

- 필수 시뮬레이터의 초기 안내는 `completion.target`과 정확히 같은 수와 대상을 안내해야 한다.
- 대체 조작 버튼은 최소 44px 높이를 유지한다.
- 잘못된 순서·잘못된 단서 선택은 학습 피드백만 주고, 활동을 중단시키거나 진행 상태를 손상시키지 않는다.
