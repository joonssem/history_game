# MUD 학습 깊이·플레이타임 확장 계획

## 1. 작성 목적

학생 테스트에서 하나의 MUD가 인터랙티브 활동을 포함하고도 약 2분 이내에 종료되는 문제를 개선한다.
목표 플레이타임은 Regular MUD 1개 기준 약 5~10분이며, 단순히 텍스트를 늘리는 것이 아니라 다음 요소가 실제 학습 행동으로 이어지도록 한다.

- 역사적 설명과 자료 탐구
- 여러 단계의 의사결정
- 결과가 드러나는 분기
- 진행을 위해 수행해야 하는 인터랙티브 활동
- 활동 결과를 다음 선택과 결말에 반영하는 피드백

이번 문서는 실행 계획서이며, 계획 승인 전에는 MUD 코드·데이터를 수정하지 않는다.

## 2. 현재 상태 진단

### 2.1 정량적 현황

전체 `data/mud/regular_*.json` 28종을 전수 집계한 결과:

| 항목 | 현재 상태 |
|---|---:|
| Regular MUD | 28종 |
| 전체 스테이지 | 181개 |
| 실제 본 진행 스테이지 | 97개 |
| MUD당 본 진행 스테이지 | 3~4개 |
| 본 진행 스테이지의 선택지 | 225개 |
| 본 진행 스테이지의 시뮬레이터 | 97개 |
| 본 진행 내러티브 문자 수 | 약 19,459자 |

나머지 84개 스테이지는 대부분 오답 선택 후 돌아오는 IF·재시도 단계이다. 따라서 정상 선택만 하는 학생은 MUD당 3~4개의 본 단계와 최종 보상만 경험한다.

### 2.2 인터랙티브 활동의 구조적 문제

[js/mudEngine.js](js/mudEngine.js)의 현재 흐름은 다음과 같다.

1. `renderStage()`가 스테이지의 시뮬레이터를 설정한다.
2. 같은 함수가 즉시 선택지 버튼을 생성한다.
3. 선택지 버튼은 시뮬레이터 완료 여부와 관계없이 활성화된다.
4. 학생은 캔버스를 누르지 않고도 다음 스테이지로 이동할 수 있다.

즉, 시뮬레이터는 현재 대부분 보조 시각화·장식 활동이며, 핵심 진행 조건이 아니다.

### 2.3 활동 난이도와 반복성 문제

[js/mudSimulators.js](js/mudSimulators.js)를 기준으로 현재 활동은 다음과 같은 편중이 있다.

- `culture-touch`: 28개 본 단계
- `text-reading`: 25개 본 단계
- `battle-gauge`: 16개 본 단계
- 그 외 특수 모드: 소수

많은 범용 모드는 터치 4회로 게이지가 100%가 되며, 클릭마다 역사적 판단이나 피드백이 발생하지 않는다. 따라서 “인터랙티브 활동 포함”이 실제 체험 시간이나 사고 시간으로 이어지지 않는다.

### 2.4 분기 깊이 문제

현재 Regular MUD의 일반적인 구조는 다음과 같다.

```text
본 단계 1 → 본 단계 2 → 본 단계 3 → 완료
              ↘ IF 재시도 → 본 단계로 복귀
```

- 오답은 주로 짧은 설명 후 원래 단계로 복귀한다.
- 정답 선택도 대부분 다음 본 단계로 직선 이동한다.
- 선택 결과에 따른 자원·관계·역사적 결과의 누적이 거의 없다.
- 분기 수는 존재하지만, 플레이 경험상 의미 있는 경로 차이는 작다.

## 3. 문제 정의

### 핵심 문제

1. 정상 루트의 단계 수가 너무 적다.
2. 시뮬레이터가 다음 진행을 요구하지 않는다.
3. 동일한 게이지·터치 패턴이 반복된다.
4. 선택지 분기가 주로 정답/오답 재시도에 머문다.
5. 인터랙티브 결과가 다음 서술·선택·결말에 누적되지 않는다.
6. 플레이타임 목표를 자동으로 검증하는 지표가 없다.

### 해결 원칙

- 텍스트만 늘려 5~10분을 만들지 않는다.
- 모든 활동을 억지로 길게 만들지 않는다.
- 단계마다 “읽기 → 판단 → 행동 → 결과 확인”의 짧은 학습 루프를 만든다.
- 분기는 많게 만들기보다 결과 차이를 분명하게 만든다.
- 기존 JSON 기반 구조와 정적 배포 방식을 유지한다.
- Regular MUD와 Deep-dive MUD의 역할을 구분한다.

## 4. 목표 지표

### Regular MUD 1개 기준

| 지표 | 목표 |
|---|---:|
| 정상 루트의 핵심 장면 | 5~7개 |
| 의미 있는 판단 지점 | 4~6개 |
| 진행 필수 인터랙티브 활동 | 3~5개 |
| 활동별 평균 수행 시간 | 30~60초 |
| 정상 플레이타임 | 5~10분 |
| 오답·IF 포함 재도전 시 | 7~12분 |
| 최소 엔딩 수 | 2개 이상 또는 결과 등급 2단계 이상 |

### 품질 지표

- 활동을 건너뛰고 선택지만 연속 클릭해도 완료되는 구조가 없어야 한다.
- 각 필수 활동은 성공 조건과 실패·재시도 피드백을 가져야 한다.
- 각 핵심 선택은 다음 내러티브 또는 자원 상태에 영향을 줘야 한다.
- 학생이 선택지 위치나 문장 길이만으로 정답을 추측할 수 없어야 한다.
- 3회 이상 반복 플레이에서 활동 종류와 선택 결과가 동일하게 느껴지지 않아야 한다.

## 5. 실행 단계

### 단계 0 — 대표 MUD 파일럿 선정

처음부터 28종을 동시에 확장하지 않는다. 다음 3종을 대표 파일럿으로 선정한다.

1. `regular_paleolithic.json`: 기본 생존·게이지 활동
2. `regular_gwangbok.json`: 투표·태극기·복수 보상 활동
3. `regular_three_kingdoms.json`: 복수 인물·전쟁 전략·복수 보상 활동

선정 이유:

- 서로 다른 시대와 기존 시뮬레이터를 대표한다.
- 파일럿에서 설계가 검증되면 다른 Regular MUD에 템플릿으로 확장할 수 있다.

### 단계 1 — 스테이지 구조 확장

대상: 파일럿 3종 → 이후 28종

각 MUD의 정상 루트를 3~4개 본 단계에서 5~7개 핵심 장면으로 확장한다.

권장 장면 구성:

1. 역사적 상황 진입과 목표 설정
2. 첫 번째 자료·환경 탐구
3. 핵심 인물 또는 집단의 선택
4. 인터랙티브 활동을 통한 문제 해결
5. 선택 결과에 따른 위기·갈등
6. 두 번째 전략·가치 판단
7. 결과 정리와 보상·성찰

각 단계는 다음 중 최소 2개를 포함한다.

- 새로운 역사적 정보
- 관찰·탐색해야 할 자료
- 선택 결과가 달라지는 행동
- 다음 단계에 영향을 주는 상태 변화
- 학생이 자신의 판단을 설명할 수 있는 질문

### 단계 2 — 분기 구조 재설계

분기 폭발을 방지하기 위해 “분기 후 수렴” 구조를 사용한다.

```text
공통 장면
 ├─ 전략 A → A 결과 장면 ┐
 └─ 전략 B → B 결과 장면 ┘ → 공통 위기 장면
```

각 파일럿 MUD에 다음을 적용한다.

- 단순 정답/오답 외에 전략 A/B 선택 1~2개 추가
- 분기별 결과 서술을 최소 1개 장면 제공
- 자원·신뢰·민심·준비도 등 1~2개의 상태값 도입
- 상태값에 따라 최종 결과 문구 또는 보상 등급 변경
- IF 단계는 오답 처벌보다 역사적 대안의 결과를 보여 주는 역할로 개선

권장 데이터 확장 예시:

```json
{
  "choices": [
    {
      "text": "백성의 안전을 우선한다",
      "next": "3a",
      "effects": { "民心": 2, "軍備": -1 }
    },
    {
      "text": "군사적 승리를 우선한다",
      "next": "3b",
      "effects": { "民心": -1, "軍備": 2 }
    }
  ]
}
```

실제 키 이름은 기존 코드 스타일과 충돌하지 않도록 구현 단계에서 확정한다.

### 단계 3 — 인터랙티브 활동을 진행 필수 요소로 전환

#### 3.1 공통 인터랙션 계약 추가

각 스테이지의 시뮬레이터에 다음 메타데이터를 선택적으로 추가한다.

```json
{
  "simulator": {
    "type": "gauge",
    "mode": "paleo-fire",
    "required": true,
    "completion": {
      "target": 100,
      "minActions": 8
    },
    "successText": "불씨가 안정적으로 살아났습니다."
  }
}
```

필수 활동일 때의 동작:

- 활동 완료 전 선택지 버튼을 비활성화하거나 안내 상태로 둔다.
- 완료 시 선택지를 활성화한다.
- 완료 여부는 `MudEngine`이 단일 방식으로 판단한다.
- 재진입 시 해당 스테이지의 활동 상태를 초기화한다.

#### 3.2 활동 유형별 개선

| 현재 유형 | 문제 | 개선 방향 |
|---|---|---|
| `culture-touch`, `text-reading` | 단순 반복 터치·게이지 | 역사 자료의 여러 지점 탐색, 순서 맞추기, 핵심 정보 찾기 |
| `battle-gauge` | 게이지 상승만으로 완료 | 병력·지형·보급을 선택하고 결과에 따라 게이지 변화 |
| `paleo-fire`, `paleo-stone` | 반복 클릭 비중 높음 | 재료 선택 → 순서 판단 → 반복 행동 → 결과 확인 |
| `gwangbok-vote`, `gwangbok-flag` | 단계 수는 있으나 선택과 연결 약함 | 투표 절차·태극기 구성의 순서를 맞히고 역사적 의미 피드백 제공 |
| `mn-*` 전투 모드 | 조작은 있으나 승리 조건이 약함 | 목표 함선·시간·탄약·피해 조건을 명시하고 성공/실패 결과 반영 |
| `slider` | 슬라이더 값이 다음 장면과 충분히 연결되지 않음 | 값에 따른 물살·노동력·효율 결과를 상태값으로 전달 |

#### 3.3 활동 시간 설계

- 단순 게이지 4회 완료는 금지한다.
- 반복 행동은 최소 6~12회가 필요하되, 매 행동에 시각·문장 피드백을 제공한다.
- 행동 수를 늘리는 것만으로 시간을 확보하지 않는다.
- 한 활동 안에 최소 1회의 판단 또는 순서 선택을 포함한다.

### 단계 4 — 내용 확장과 교육적 검수

파일럿 MUD마다 다음 자료를 추가한다.

- 핵심 역사 자료 2~3개
- 인물·집단 관점이 드러나는 대사 2개 이상
- 학생이 관찰할 수 있는 단서 3개 이상
- 오답의 역사적 결과를 설명하는 IF 장면
- 마지막에 “왜 그렇게 선택했는가”를 묻는 성찰 질문

검수 기준:

- 5학년 사회 교육과정의 핵심 개념과 직접 연결되는가
- 사실·연대·인물 관계가 정확한가
- 현재형 게임 연출이 역사적 사실을 왜곡하지 않는가
- 정답 하나만 암기하는 대신 근거를 보고 판단하게 하는가
- 선택 결과가 역사적 맥락에 맞는가

### 단계 5 — 파일럿 측정 후 전체 확장

파일럿 3종을 먼저 배포하지 않은 로컬 테스트 상태에서 측정한다.

측정 항목:

- 시작부터 완료까지 시간
- 각 단계 체류 시간
- 인터랙션 행동 수
- 활동을 건너뛰려 한 횟수
- 정답·오답·분기 선택 비율
- 학생이 막힌 지점
- 완료 후 기억나는 역사 개념

목표를 충족하면 다음 순서로 확장한다.

1. 선사·고조선·삼국
2. 고려
3. 조선 전기·후기
4. 개항기·일제강점기
5. 광복·전쟁·현대

## 6. 구현 순서와 변경 범위

### 1차 구현

- `js/mudEngine.js`: 필수 활동 상태, 선택지 잠금·해제, 공통 완료 판정
- `js/mudSimulators.js`: 활동별 완료 이벤트와 피드백 표준화
- 파일럿 JSON 3종: 5~7개 본 단계, 활동 메타데이터, 분기·상태값
- `scripts/03_validate_mud_integrity.py`: 필수 활동 계약·도달성·분기 상태 검증

### 2차 구현

- 파일럿 검증 결과에 따라 공통 활동 컴포넌트 보강
- 25종 Regular MUD를 시대별 템플릿으로 확장
- Deep-dive 4종은 별도의 10~15분 심화 목표로 재측정

### 변경하지 않을 것

- MUD 전체 엔진 전면 재작성
- 기존 JSON 기반 구조 폐기
- 모든 선택지를 독립적인 완전 분기로 만드는 구조
- 단순 텍스트 반복으로 플레이타임을 늘리는 방식

## 7. 검증 계획

### 정적 검증

```bash
python scripts/01_validate_game_data.py
python scripts/03_validate_mud_integrity.py
node --check js/mudEngine.js
node --check js/mudSimulators.js
git diff --check
```

추가 검증 항목:

- `required: true` 활동에 완료 조건이 존재하는가
- 필수 활동 완료 전 선택지로 진행할 수 없는가
- 모든 분기 대상 스테이지가 존재하는가
- 상태값이 정의되지 않은 키를 쓰지 않는가
- 모든 정상 루트가 최소 5개 핵심 장면을 갖는가

### 브라우저 검증

1. 활동을 수행하지 않고 선택지를 눌렀을 때 진행이 차단되는지 확인
2. 최소 행동 수를 채우면 선택지가 활성화되는지 확인
3. 활동 실패·재시도 시 상태가 초기화되는지 확인
4. 정상·대안 분기의 내러티브가 서로 다른지 확인
5. 보상과 성찰 일기가 모든 엔딩에서 정상 동작하는지 확인
6. 모바일 터치와 데스크톱 마우스 입력을 각각 확인
7. 파일럿 3종의 실제 플레이 시간을 측정

## 8. 위험 요소와 대응

| 위험 | 대응 |
|---|---|
| 활동 강제 때문에 학생이 답답함을 느낌 | 진행 잠금 이유·완료 조건·현재 진행률을 명확히 표시 |
| 반복 터치로 플레이타임만 늘어남 | 활동마다 판단·피드백·역사 단서를 포함 |
| 분기 증가로 JSON 유지보수 악화 | 분기 후 수렴, 공통 상태 키, 템플릿 사용 |
| 기존 MUD 보상·도감 회귀 | 기존 무결성 검사와 보상 ID 검증 유지 |
| 기기별 캔버스 입력 차이 | 마우스·터치 공통 이벤트와 브라우저 테스트 병행 |
| 5~10분 목표가 학생별로 크게 달라짐 | 평균뿐 아니라 최소·최대·중앙값을 측정 |

## 9. 완료 기준

파일럿 3종 모두 다음을 만족해야 전체 확장을 승인한다.

- 정상 플레이 중앙값 5~10분
- 핵심 장면 5개 이상
- 진행 필수 인터랙티브 활동 3개 이상
- 활동을 건너뛰어도 완료되는 경로가 없음
- 의미 있는 분기 2개 이상
- 정적 검증·브라우저 회귀 테스트 통과
- 학생 테스트에서 “무엇을 눌렀는지”가 아니라 “왜 그렇게 선택했는지” 설명 가능

## 10. 현재 권장 다음 작업

1. 파일럿 `regular_paleolithic.json`의 상세 설계안 작성
2. 공통 `required` 활동 계약을 먼저 구현
3. 구석기 MUD의 5~7장면 버전 제작
4. 로컬 브라우저에서 3회 이상 플레이타임 측정
5. 결과 승인 후 광복·삼국 MUD에 확장

## 11. 파일럿 실행 결과

2026-08-26 현재 `regular_paleolithic.json`에 1차 파일럿을 적용했다.

- 정상 핵심 장면을 기존 4개에서 6개로 확장
- 사냥 전략과 공동체 협력 분기 추가
- `4-1`, `5-1` IF 재시도 장면 추가
- 필수 인터랙티브 활동 5개 적용
- 활동별 최소 행동 수: 5회, 8회, 10회, 8회, 8회
- 활동 완료 전 선택지 버튼 잠금 확인
- 활동 완료 후 선택지 활성화 확인
- 정상 선택 루트에서 단계 1→2→3→4→5→6→성찰 완료 확인
- JSON·MUD 무결성·JavaScript 문법 검사 통과
- 브라우저 콘솔 오류 0건

현재 구현은 파일럿 검증 단계이다. 다른 27종 Regular MUD에는 아직 필수 활동 계약을 적용하지 않았으며, 파일럿 플레이타임을 실제 학생 또는 테스트 사용자로 측정한 뒤 다음 MUD로 확장한다.
