# 유물 도감(`data/artifacts.json`) 36종 사실 대조 — 라운드 4 §2-1

- 작성: Sonnet 5(대조실), 2026-09-15
- 지시서: `docs/handoff/claude_four_track_round4_instruction.md` §2-1
- 범위: `data/artifacts.json`의 `name`/`desc`/`hint`/`era`/`tierName` 36개 전체를 훑고, 그중 **실제로 공식 출처 원문을 열어 대조한 항목**과 **아직 안 연 항목**을 모두 표에 남긴다. `art_12`의 `era`는 사용자 판단 대기라 손대지 않았다(§0-5). 3단원(일제강점기·전쟁·광복) 콘텐츠는 지시대로 수정하지 않고 보고만 한다.

## 정정·완화한 항목 (원문 확인함)

`data/artifacts.json`은 학생이 도감에서 직접 읽는 `desc`와, 화면에 렌더링되지 않는 개발자용 `sourceNote`를 구분하지 않던 스키마였다. 처음에는 확인 내용을 `desc`에 바로 적었는데, `scripts/11_audit_artifacts.py`가 "설명 150자 초과"로 4건 모두 경고했다 — 5학년이 읽는 카드에 각주·URL까지 붙이는 건 부적절하다고 판단해, `desc`는 간결하게 유지하고 확인 내용은 새 `sourceNote` 필드(`artifactComparisons.json`과 같은 이름 규칙, 화면에 안 뜸)로 옮겼다.

| id | 대상 | 문제 | 대조 결과 | 조치 | 근거 URL·확인일 |
|---|---|---|---|---|---|
| `art_7` | 귀주대첩 승전보와 강감찬 보검 | "강감찬 보검"이라는 구체적 실물이 있는 것처럼 서술 | **확인 불가** — 국립중앙박물관 소장품 검색·웹 검색으로 강감찬의 검이나 승전보 원본을 찾지 못함(찾은 것은 별개의 「귀주대첩도」 보물 588호뿐) | `sourceNote` 신설 — 실물 미확인·상징적 표현임을 기록. `desc`는 원래 톤 유지(단정 표현 없었으므로 그대로) | museum.go.kr 소장품 검색, 웹 검색(국사편찬위원회 계열) 2026-09-15 |
| `art_9` | 태조 왕건 훈요 10조 | "고려의 통치 지침"이라며 원본 문서가 있는 듯 서술 | **확인 불가(원본 부존재 확인)** — 한국민족문화대백과사전이 원본 실물의 존재를 확인할 수 없고 『고려사』를 통해서만 내용이 전해진다고 명시 | `desc`에 "남겼다고 전하는"으로 한 단어 완화 + `sourceNote`에 원본 부존재 사실 기록 | encykorea.aks.ac.kr/Article/E0065813 2026-09-15 |
| `art_10` | 고려 과거 합격증 '홍패' | era를 "고려 시대"로 두고 실물이 고려 것인 양 서술 | **오류** — museum.go.kr 소장품 상세(relicId=2374)가 국립중앙박물관 소장 홍패는 1814년(순조14, 조선) 조기영 홍패(신수14948)라고 명시. 고려 시대 홍패 원본은 개인·문중 소장뿐(2026-09-08 유물비교 작업 때도 같은 결론) | `desc`는 "제도가 고려 때 시작됐음을 보여준다"로 정정(실물 시대를 단정하지 않음) + `sourceNote`에 실물의 실제 시대를 명시. `era`/`hint`는 MUD([고려 사회 탐정])와 연결돼 있어 이번 범위(대조실은 `data/mud/*.json` 읽기만) 밖 — 손대지 않음, 관문 설계실에 참고 보고 | museum.go.kr/site/main/relic/search/view?relicId=2374 2026-09-15 |
| `art_14` | 홍대용의 둥근 지구의 | "지구의"라는 구체적 실물이 전해지는 것처럼 서술 | **확인 불가/개념 혼동** — 홍대용이 실제로 만든 것으로 알려진 기구는 1762년 나경적과 함께 만든 '혼천시계'이고, 이 원본도 전하지 않아 국립중앙과학관이 문헌 기록으로 현대에 복원함. "지구의" 실물을 확인하지 못함 | `desc`를 "지전설을 나타내는 상징적인 물건"으로 완화(실학의 과학적 상징이라는 단정 제거) + `sourceNote`에 실제 발명품(혼천시계)도 원본이 없고 현대 복원임을 기록 | 국립중앙과학관 혼천시계 복원 보도(아틀라스뉴스 등), 우리역사넷 2026-09-15 |

## 확인함(변경 없음, 원문 확인함)

| id | 대상 | 대조 결과 | 근거 URL·확인일 |
|---|---|---|---|
| `art_22` | 경복궁 근정전 박석 — "정도전의 설계" | **일치** — 태조가 도읍 설계 총책임을 정도전에게 맡겼고 근정전 등 건물 이름도 정도전이 지음(실제 시공은 심덕부 등이 담당). "설계"라는 표현이 통상적 서술과 부합 | 위키백과·우리역사넷 계열 검색 결과 종합, 2026-09-15(단, §0-3 규칙상 위키백과 자체는 근거로 쓰지 않았고 우리역사넷 인용 부분만 근거로 삼음) |

## 확인 불가 — 이번 라운드에서 원문을 열지 못한 항목 (다음 라운드 후보)

아래는 이번 패스에서 **원문을 실제로 열어 대조하지 않았다.** 표현 자체가 이미 온건하거나(복합 카드, 생활사 유물), 시간 제약으로 미루거나, 3단원이라 손댈 수 없는 항목이다. 오류로 단정하지 않는다 — 단지 "확인했다"고도 적지 않는다.

| id | 대상 | 비고 |
|---|---|---|
| `art_1` | 연천 전곡리 주먹도끼 | 2026-09-08 유물비교 작업 때 findspot(연천군)을 확인한 적은 있음(다른 맥락) |
| `art_3` | 고창 탁자식 고인돌 | "수십 톤" 표현 — 고창 고인돌은 10톤 미만~300톤까지 편차가 커 "수십 톤"이 대표성 있는지 미확인 |
| `art_5`, `art_6` | 백제 칠지도, 신라 북한산 순수비 | 교과서 수준 통설(하사설 vs 헌상설 논쟁 있음, "보낸"이라는 단순화 표현) — 이번엔 재검증 안 함 |
| `art_8` | 직지심체요절 영인본과 고려 비색 청자 | "세계 최고(最古) 금속활자본"은 UNESCO 세계기록유산 등재로 이미 잘 알려진 사실이라 낮은 우선순위로 미룸 |
| `art_11`, `art_13`, `art_19`, `art_21`, `art_25`~`art_28`, `art_sejong` | 조선 후기·통일신라·발해·삼국 생활사 유물 다수 | 각 항목의 개별 소장처·명칭까지는 이번 라운드에서 원문 대조 안 함 |
| `art_23` | 한양 전차 승차권과 근대 전등 모형 | "모형"이라는 표기가 이미 실물이 아님을 밝히고 있어 우선순위 낮게 판단, 원문 미대조 |
| `art_deep_2`, `art_deep_3`, `art_deep_4` | Deep-dive 보상(화랑 서약검, 거중기 모형, 민주 시민 헌장 등) | `art_deep_1`("8조법 목판")과 같은 유형의 상징적 서술 가능성이 있음(관창 맹세검 등) — 다음 라운드에서 `art_deep_1`과 같은 기준으로 점검할 만하다고 보고만 함 |

## 3단원(수정 금지, 보고만) — `hint` 차시 번호 확인

`art_15`~`art_20`, `art_24`, `art_independence`, `art_korean_war` 8종은 지시서 §0-4에 따라 **고치지 않았다.** `hint`의 "N단원 M차시" 표기가 실제와 다른지도 이번엔 대조하지 않았다(3단원 콘텐츠 자체를 열람하지 않음). 다음에 3단원 민감 주제 기준이 확정되면 이 8종을 §2-1과 같은 방식으로 대조할 것을 제안한다.

## `era` 변경 없음 — 나의 연표 영향 확인

이번 라운드에서 수정한 4개 항목(`art_7`/`art_9`/`art_10`/`art_14`) 모두 **`era` 필드는 바꾸지 않았다**(desc만 완화·정정). 따라서 `js/miniGames.js`의 `getArtifactTimelineRange()` 판정에는 영향이 없다 — era 문자열이 그대로라 기간표 조회 결과도 그대로다. `art_12`의 `era`도 지시대로 그대로 두었다.

## 검증

- `python -c` JSON 파싱: 36개 항목 그대로.
- `python scripts/01_validate_game_data.py`: 통과(신규 유적 필드 검사 포함, `data/artifacts.json`은 이 스크립트가 개수만 확인).
- `python scripts/11_audit_artifacts.py`: 처음엔 4건이 "설명 150자 초과"로 걸려(`desc`에 각주를 직접 넣었던 1차 편집분), `sourceNote`로 분리한 뒤 재실행해 **0건**으로 확인.
- `js/`는 전혀 건드리지 않았다.

## 라운드 5 추가 — R3-08·R3-09 정정 + 도감 미대조 항목(`art_23`, `art_deep_2~4`)

- 작성: Sonnet 5(대조실), 2026-09-15
- 지시서: `docs/handoff/claude_four_track_round5_instruction.md` §2
- 먼저 `docs/audits/round3_red_team_audit.md`와 Codex T2의 `docs/audits/source_crosscheck_followup_20260914.md`를 읽었다 — T2는 `cmp_folk_paintings_1`/`cmp_paleo_neo_1`/`cmp_metal_tech_1`(T2-2)와 `art_12`(T3, 보고 전용)를 처리했지만 **R3-08·R3-09는 처리하지 않고 대조실 몫으로 남겨 뒀다.** 이번에 직접 처리했고, **Codex T2의 정정은 되돌리지 않았다**(§2-5).

### R3-08 — `cmp_crown_1.artifactB` 가야 금동관 연대

- red team 판정: "6세기 전반"의 출처인 encykorea 글이 실제로는 **지산동 32호분 금동관이 아니라 리움미술관·도쿄국립박물관 소장 전세 가야 금관**을 설명하는 문장이었다(대상 오인).
- WebFetch로 `encykorea.aks.ac.kr/Article/E0068696` 원문을 다시 직접 열어, 지산동 32호분 금동관을 설명하는 문장("가야문화를 대표하는 작품이며 제작 의장에서 가야 금관과 공통하는 면모를 보여준다")에는 **구체적 세기 표기가 없음**을 확인했다.
- 웹 검색으로 지산동 고분군 전체의 축조 시기(4세기 말~6세기 중엽)는 확인했지만 32호분 하나만 좁혀 특정할 근거는 찾지 못했다.
- **조치**: `era`를 "가야 시대(대가야, 6세기 전반)" → **"가야 시대(대가야, 5~6세기경으로 짐작)"** 로 되돌렸다. 라운드 3 이전 값과 문자열은 비슷해 보이지만, 이번엔 "확정 근거가 없다는 사실을 확인한 뒤 의도적으로 넓게 유지"한 것이라는 점을 `sourceNote`에 남겼다.
- 확인 대조: **확인 불가(정정)** — 원문을 열었으나 대상이 다른 문장이었음을 확인하고 완화.

### R3-09 — `cmp_ceramics_1`/`cmp_ceramics_2`의 청자 매병 연대

- red team 판정: 데이터는 "12세기경", 그러나 sourceNote가 근거로 든 큐레이터 글(relicRecommendId=254445)의 **실제 이미지 캡션은 "고려 12세기 후반~13세기"** — 라운드 3에서 본문의 다른 문장("12세기 고려 청자 매병을 대표할 만하다")만 보고 캡션을 확인하지 않은 인용 위치 오류였다.
- WebFetch로 해당 URL을 다시 직접 열어 캡션 전문("청자 구름 학 무늬 매병, 고려 12세기 후반~13세기, 높이 30.0cm, 보물, 덕수2182")을 확인했다.
- **조치**: `cmp_ceramics_1.artifactA.era`, `cmp_ceramics_2.artifactA.era`를 "고려 시대(12세기경)" → **"고려 시대(12세기 후반~13세기)"** 로 정정. 두 sourceNote 모두에 재확인 경위를 남겼다.
- 대조 결과: **일치(원문 캡션 그대로 반영)**.
- 비교 쌍 판단 근거 흔들림 여부: `cmp_ceramics_1`의 claim은 "고려"/"조선"만 빈칸으로 채우고 세기는 쓰지 않으므로 **claim·정답에는 영향 없음**. `traits`·`observation`·`evidenceCards`도 세기를 언급하지 않아 무영향.

### 라운드 4 미대조 항목 — `art_23`, `art_deep_2~4` (`art_deep_1`과 같은 기준)

`data/artifacts.json`에서 확인:

| id | 문제 | 조치 |
|---|---|---|
| `art_deep_2` | **name/desc 불일치 발견** — name은 "화랑의 서약검과 정혜공주 묘지석", desc는 "화랑 관창의 맹세검과 해동성國 발해 고왕 대조영의 건국 보검"으로 서로 다른 두 번째 유물을 가리킴 | name 기준으로 desc를 "정혜공주 묘지석"으로 정정. `sourceNote`에 관창의 검은 상징적 표현(실물 미확인, 『삼국사기』 일화는 실재)이고 정혜공주 묘지석은 실존 발해 유물이나 중국 지린성 소재(국내 미소장)임을 기록 |
| `art_deep_3` | **name/desc 불일치 발견** — name은 "훈민정음 해례본과 수원 화성 설계도", desc는 "춘추관 사초와 거중기 모형"으로 서로 다른 두 유물을 가리킴 | name 기준으로 desc를 "수원 화성의 성곽 축조 계획"으로 정정. `sourceNote`에 훈민정음 해례본(국보, 간송미술관)은 실존, '설계도'는 화성성역의궤 등을 상징한 표현임을 기록 |
| `art_deep_4` | name·desc는 일치하나 "민주 시민 헌장(직선제 헌정 선언문)"이라는 구체적 문서명을 확인 못함 | `sourceNote`로 실물 미확인·상징적 표현임을 기록(desc는 변경 없음) |
| `art_23` | "한양 전차 승차권"·"근대 전등 모형" 개별 소장품 미확인. desc 자체는 이미 개별 소장품을 단정하지 않고 일반화돼 있어 안전 | `sourceNote`로 개별 소장품 미확인 사실과, 전차(1899)·전등(1887) 도입 자체는 널리 알려진 사실임을 구분해 기록(desc는 변경 없음) |

**`art_deep_2`/`art_deep_3`의 name/desc 불일치는 `art_deep_1`("8조법 목판", 이전 라운드에 정정)과 같은 유형이 아니라 한 단계 더 심각한 문제였다** — 단순히 실물 단정이 과했던 게 아니라 **서로 다른 두 유물을 가리키는 데이터 자체의 모순**이었다. `art_deep_1`을 고칠 때 "혹시 다른 항목도 이런 불일치가 있을까" 우려했다가 그때는 없었다고 결론 냈는데, 이번에 실제로 두 건이 나왔다 — 다음에 유물 카드를 새로 만들 때는 name/desc가 같은 대상을 가리키는지부터 확인하는 습관이 필요하다.

### 검증(라운드 5 추가분)

- `python -c` JSON 파싱: `data/artifactComparisons.json`(11개), `data/artifacts.json`(36개) 둘 다 정상.
- `python scripts/01_validate_game_data.py`: 통과(유적 필드 검사 포함).
- `python scripts/11_audit_artifacts.py`: **0건**(4개 항목에 `sourceNote`를 추가했지만 `desc` 자체는 이미 짧게 유지해 150자 초과 없음).
- `python scripts/04_validate_mud_contract.py`, `node scripts/05_test_simulator_runtime.js`: 통과(내 변경과 무관한 영역이지만 지시서 §0-7 요구대로 실행).
- `node scripts/13_audit_inquiry_combinatorics.js`, `node scripts/14_lint_inquiry_semantics.js`: 실행하면 `docs/audits/inquiry_combinatorics_audit.md`·`inquiry_semantics_lint.md`(조작대 소유)가 재생성되는데 **실제 내용 변화는 없었다**(git diff 무변화, 줄바꿈 경고만) — 커밋 범위를 깨끗하게 유지하려고 `git checkout --`으로 되돌렸다.
- `js/`는 이번에도 전혀 건드리지 않았다.

## 다음에 볼 것

- `art_7`/`art_9`/`art_10`/`art_14`의 늘어난 `desc`가 도감 상세 모달에서 실제로 깨지는지 `artifact-site`(8802)로 확인했다 — `art_10`으로 스크린샷까지 확인, 레이아웃 정상·콘솔 에러 0건(`js/` 미변경이라 필수 요건은 아니었지만 제출 전 직접 확인함).
- "확인 불가" 표의 항목들을 다음 라운드에서 이어서 대조한다.

## 라운드 10 — 3단원 유물 9종 재검토 (D-032 해제 뒤)

라운드 5에서 "수정 금지, 보고만"으로 묶여 있던 3단원(일제강점기·전쟁·광복) 유물 9종을 이번 라운드에서 처음 고쳤다. 대상: `art_15`, `art_16`, `art_17`, `art_18`, `art_20`, `art_24`, `art_independence`, `art_korean_war`, `art_deep_4`. (라운드 5 보고서의 "art_15~art_20, art_24, art_independence, art_korean_war 8종"은 `art_19`가 실제로는 2단원 병자호란 콘텐츠라 3단원이 아니었음을 이번에 확인했다 — `art_19`의 `hint`는 "2단원 7차시"로 애초부터 정확했다. 지시서가 준 9종 목록(`art_deep_4` 포함)이 정확한 최종 목록이다.)

### 실물 여부 판정 결과

| id | 판정 | 근거 |
|---|---|---|
| `art_15` (5·10 총선거 투표함) | **상징적 표현** | 1948년 실제 투표함의 소장처를 확인하지 못함. 대한민국역사박물관 특별전(2016)에서 전시된 목재 투표함은 1969년 삼선개헌 국민투표용으로 확인되어 대상 시기가 다름. desc를 "투표함을 상징하는 자료"로 수정 |
| `art_16` (제헌헌법 원본) | **실물이 있는 유물** | 원본(필사본)은 대통령기록관 보존, 제정 초고(유진오 초고)는 고려대 박물관 소장 · 국가지정기록물 제1호. desc 변경 없음, `sourceNote`만 추가 |
| `art_17` (대한광복회 포고문과 서명록) | **상징적 표현** | '포고문'·'서명록' 명칭의 구체적 실물은 확인하지 못함. 국가유산포털에 등록된 대한광복회 관련 실물은 '박상진 의사 옥중 편지 및 상덕태상회 청구서'로 다른 자료다. desc를 "…독립운동을 상징하는 기록"으로 수정 |
| `art_18` (조선말 큰사전 원고) | **실물이 있는 유물** | WebFetch 직접 확인: 국가유산포털에 독립기념관 소장분(5책, 2012.12.24 국가등록문화유산). 전체 17책이 2020년 국보로 승격됐다는 내용은 검색 종합으로만 확인(원문 재확인 못함, `sourceNote`에 구분 기재). desc 변경 없음 |
| `art_20` (이산가족을 찾습니다 + 천막 교실 몽당연필) | **절반만 실물** | '이산가족을 찾습니다' 기록물은 2015년 유네스코 세계기록유산(실물). '천막 교실 몽당연필'은 구체적 소장품을 확인 못해 desc에서 '몽당연필' 명칭을 빼고 "전후 복구기 교실의 기억을 상징하는"으로 일반화 |
| `art_24` (안중근 단지 태극기·유묵) | **실물이 있는 유물** | 유묵 '일일부독서구중생형극'은 WebFetch 직접 확인(국가유산포털, 보물, 1972.08.16, 동국대 박물관 소장). 단지 태극기는 검색 종합으로 독립기념관 소장 확인(원문 재확인 못함). desc 변경 없음 |
| `art_independence` (기미독립선언서·임시헌장) | **실물이 있는 유물** | 독립기념관 소장본 2종 등 다수 원본 현존, 국가지정기록물 제12호. '임시헌장' 개별 실물은 이번에 직접 확인 못함(`sourceNote`에 기록). desc 변경 없음 |
| `art_korean_war` (수복 서울 태극기·무공훈장) | **절반만 실물** | 무공훈장은 실제 수여된 훈장으로 실물 다수. '수복 서울의 태극기'는 특정 소장품을 확인 못함 — 전쟁기념관이 소장한 실제 태극기는 1953년 철원·임진강에서 미군에게 전달된 '피로 물든 태극기'로 서울 수복(1950.9.28)과는 다른 유물. desc를 "…전투를 상징하는 태극기"로 수정 |
| `art_deep_4` (광복군 태극기·민주 시민 헌장) | 라운드 5 판정 유지 | 태극기는 실물, '민주 시민 헌장'은 상징적 표현 — 이번 라운드에서 재검토했으나 새 근거를 찾지 못해 기존 `sourceNote` 그대로 둠 |

### `hint` 차시 번호 대조 결과

9종 중 7종의 `hint` 차시 번호는 `data/lesson_track.json`(관문 설계실 소유, 라운드 9 검증 완료)과 일치했다. **2종에서 불일치를 발견해 고쳤다:**

- `art_independence`: `hint`가 "3단원 38~39차시"였으나 `lesson_track.json`(order 24, `regular_independence`, `lessonNumbers: [4,5]`)과 대조하면 "4~5차시"가 맞다. "4~5차시"로 수정.
- `art_korean_war`: `hint`가 "3단원 44~46차시"였으나 `lesson_track.json`(order 27, `regular_korean_war`, `lessonNumbers: [10,11,12]`)과 대조하면 "10~12차시"가 맞다. "10~12차시"로 수정.

**중요**: 이 두 오류는 `data/artifacts.json`만의 문제가 아니다. `data/mud/regular_independence.json`과 `data/mud/regular_korean_war.json`의 `header.tag` 필드 자체가 각각 "3단원 38~39차시"·"3단원 44~46차시"로 되어 있고, `artifacts.json`의 `hint`는 그 값을 그대로 옮긴 것으로 보인다. 3단원 전체는 14차시(index.html 탭 라벨 기준)뿐이라 38~39, 44~46은 명백히 범위를 벗어난다. **`data/mud/**` 파일은 내 소유가 아니라 고치지 않았다** — 관문 설계실(§1 "로드맵 라벨이 실제 관문 내용과 맞는지도 함께 본다")에 넘긴다. `BACKLOG.md`에도 같은 내용을 남긴다.

### 유물 비교 활동(artifactComparisons.json)

지시대로 3단원 쌍을 새로 만들지 않았다. `data/artifactComparisons.json`은 이번 라운드에 전혀 건드리지 않았다(`eraGroup`은 여전히 prehistoric_ancient/three_kingdoms/goryeo/joseon 4종뿐).

### 확인 불가로 남긴 항목

- `art_17`의 대한광복회 포고문·서명록 실물 존재 여부(부재 증명이 아니라 미확인)
- `art_20`의 '천막 교실 몽당연필' 구체 소장품 여부
- `art_independence`의 '임시헌장'(대한민국 임시헌장, 1919) 개별 실물 소장처
- `art_24`의 '단지 태극기' 소장 사실(독립기념관 소장 언급은 검색 종합, 원문 직접 열람으로 재확인 필요)
- `art_18`의 2020년 국보 승격 여부(검색 종합만, 원문 미확인 — 실물 존재 자체는 2012년 등록문화유산 지정으로 이미 WebFetch 확인됨)

### 원문 직접 열람 vs 검색 종합 구분

이번 라운드에서 WebFetch로 원문을 직접 연 항목은 `art_18`(조선말 큰사전 원고, 국가유산포털)과 `art_24`(안중근 유묵, 국가유산포털) 두 건이다. 나머지는 WebSearch 결과 종합이며, 위 표와 `sourceNote`에 각각 구분해 적었다.

### 검증(라운드 10)

- `python -c` JSON 파싱: `data/artifacts.json`(36개) 정상.
- `python scripts/01_validate_game_data.py`: 통과.
- `python scripts/04_validate_mud_contract.py`: 통과.
- `node scripts/05_test_simulator_runtime.js`: 통과.
- `python scripts/11_audit_artifacts.py`: 0건(9개 항목에 `sourceNote`를 추가했지만 `desc`는 모두 150자 이내로 유지).
- `node scripts/16_validate_lesson_track.js`: 통과(3단원 7편 order 22~28 연속 확인, 이번 hint 수정의 근거로 사용).
- `js/`, `apps/**`, `cooperative-mud/**`는 전혀 건드리지 않았다.
- `git status --short`: `data/artifacts.json` 한 파일만 변경됨을 커밋 직전 확인했다.
