# DECISIONS

프로젝트의 변경에 영향을 주는 결정만 기록한다. 새 결정은 날짜와 근거를 추가하고, 기존 결정을 뒤집을 때는 후속 결정으로 남긴다.

## D-001 — MUD 시나리오는 JSON으로 분리한다

- 상태: 채택
- 근거: 서사 콘텐츠와 실행 엔진을 분리하면 대량 콘텐츠 수정·검증이 가능하고 코드 변경을 줄일 수 있다.
- 규칙: 새 MUD는 `data/mud/*.json`과 `data/mud/_index.json` 메타데이터를 함께 갱신하고 JSON·계약 검증을 통과한다.

## D-002 — 프런트엔드는 Vanilla JS 정적 구조를 유지한다

- 상태: 채택
- 근거: 별도 빌드·서버 없이 교실 기기에서 실행하고 GitHub Pages에 배포하기 위한 단순성과 이식성을 우선한다.
- 규칙: 프레임워크나 패키지는 현재 범위의 문제를 해결하는 근거가 있을 때만 검토한다.

## D-003 — GitHub Pages를 기본 배포 대상으로 한다

- 상태: 기존 현재 운영 결정 (목표 배포 경로는 D-016으로 보완)
- 근거: 정적 HTML/CSS/JS와 JSON을 별도 백엔드 없이 배포할 수 있다.
- 규칙: 외부 런타임 비밀키·서버 상태를 전제하지 않으며, 배포 전 정적 자산·JSON·문법 검증을 실행한다.

## D-004 — Regular와 Deep-dive를 다른 교육 목적의 계층으로 구분한다

- 상태: 채택 (시간 기준은 D-007로 갱신)
- 근거: Regular는 짧은 복습, Deep-dive는 여러 차시를 연결하는 확장 탐구이므로 같은 시간·성공 기준으로 평가하면 안 된다.
- 규칙: 카탈로그·PRD·감사에서 두 계층의 목표 시간, 안내, 품질 기준을 분리한다.

## D-005 — 교육과정 정합성은 별도 검토 게이트로 둔다

- 상태: 채택
- 근거: 2015 개정 차시 기준선, 2022 개정 성취기준, 출판사별 교과서 차시는 서로 다른 주장이다.
- 규칙: 매핑 보고서와 사용 교과서 확인 상태를 구분하고, 보조 맥락 MUD를 직접 성취기준 과제로 과장하지 않는다.

## D-006 — 문서의 역할을 분리한다

- 상태: 채택
- 규칙: `PRD.md`는 요구사항, `BACKLOG.md`는 미완료 검토 항목, `DECISIONS.md`는 장기 설계 결정, `project_context.md`는 현재 인수인계, `walkthrough.md`는 완료 기록, `agents.md`는 협업 절차를 담당한다.

## D-007 — Regular MUD 시간 기준을 10분 이내·약 9분 목표로 둔다

- 상태: 채택
- 근거: 기존 5분 안팎의 분량은 학생이 탭 가능한 요소를 의미 없이 빠르게 누를 때 1~2분 안에 종료되는 관찰이 있었다.
- 규칙: Regular의 상한은 10분 이내로 유지하고, 정상적인 자료 읽기·선택·상호작용을 포함한 설계 목표는 약 9분으로 둔다. 무작위 탭만으로 단축되는 경로는 별도 품질 검토 대상이며, 실제 학생 측정으로 확정한다.

## D-008 — MUD 등록 차시는 화면 표시 기준으로 통일한다

- 상태: 채택
- 근거: 커리큘럼 원본은 전역 차시 번호를 저장하지만 화면의 `lessonDisplay`와 `app.js` 인덱스 조회는 단원 내부 차시 번호를 사용한다. 두 기준을 섞으면 인덱스 우선 등록이 작동하지 않는다.
- 규칙: `_index.json`과 MUD JSON의 등록용 `lessonNumbers`는 화면에 표시되는 단원별 차시 번호로 기록한다. MUD 내부 설명·출처의 전역 차시 표기는 별도 콘텐츠 검토 대상으로 두며, 인덱스에 없는 차시를 위한 fallback은 당분간 유지한다.

## D-009 — Regular MUD 엔딩 전 성찰은 필수 활동으로 둔다

- 상태: 채택 (대표 수직 슬라이스 적용)
- 근거: 제품 흐름이 역사적 결과 확인에서 끝나지 않고 학생의 판단과 근거를 되돌아보는 활동까지 포함해야 한다.
- 규칙: 엔딩 직전 성찰 시뮬레이터는 선언된 핵심 단서를 확인해야 완료되며, `required: true` 여부와 완료 조건을 MUD별 JSON에 명시한다. 실제 태블릿 흐름 검증 후 전체 MUD 적용 여부를 결정한다.
## D-010 — Regular MUD 주·보조 등록 구분

- **결정**: 교육과정 기본 차시 버튼은 `primary` MUD 하나만 사용하고, 교육 보완·확장 성격의 MUD는 `supplementary`로 등록한다.
- **현재 적용**: 2단원 7차시는 조선 외교를 주 MUD로, 명량해전은 보조 MUD로 둔다. 3단원 12차시는 전후 재건을 주 MUD로, 6·25 전쟁은 보조 MUD로 둔다.
- **이유**: `unitId + lessonNumbers`가 겹칠 때 배열 순서에 따라 포털 버튼이 달라지는 문제를 막고, 교과서 차시와 보조 역사 맥락을 구분하기 위해서다.
- **후속**: 보조 MUD를 별도 확장 활동 영역에 노출할 필요가 있는지 브라우저 확인 때 검토한다.

## D-011 — 근현대 민감 주제 장면은 기록·추모 중심으로 표현한다

- **상태**: 채택 (Phase 38 및 `regular_japanese_rule_2` 교차 검증 승인)
- **근거**: 피해를 직접 재현하거나 폭력 도상으로 소비하지 않고, 학생이 기록·증언·추모 자료를 관찰하며 역사적 맥락을 이해하도록 해야 한다.
- **규칙**: 근현대 장면에서는 군인·무기·전투·피해 재현을 기본 도상으로 사용하지 않는다. 문서·원고·기록·촛불·꽃·평화 상징처럼 자료와 기억을 나타내는 요소를 우선한다.
- **콘텐츠 규칙**: `instruction`·`infoText`·`feedback`·`narrative`·`glossary`는 사실성과 초등학생 적합성을 함께 검토한다. 민감한 용어는 사실을 훼손하지 않는 범위에서 연령에 맞게 표현하고, 새 사례·수치·증언을 근거 없이 추가하지 않는다.
- **상호작용 규칙**: 학생을 피해자·가해자 역할로 수행시키거나 피해자의 감정 반응을 정답으로 요구하지 않는다. 자료 확인과 비교를 완료 조건으로 사용한다.

## D-012 — 실제 교실을 핵심 검증 환경으로 둔다

- **상태**: 채택 (2026-09-01)
- **근거**: 1단원 2·3차시 연속 활동이 학생들에게 4분 이내에 완료되었고, 학생들이 선택지 편향과 콘텐츠 오류를 직접 발견했다.
- **규칙**: 설계 추정치만으로 활동 시간과 난이도를 확정하지 않는다. 실제 수업의 행동 관찰·학생 의견·최소 진단 기록을 함께 보고 작은 수정 단위로 재검증한다. 교실의 대화와 전략 공유가 가능한 집단 환경도 고려한다.

## D-013 — 플레이 진단은 개인정보 없는 최소 기록으로 시작한다

- **상태**: 채택 원칙 (구현 전)
- **근거**: 빠른 완료의 원인이 선택지 난이도, 시뮬레이터 완료 조건, 읽기 속도, 교실 정보 공유 중 무엇인지 관찰만으로는 구분하기 어렵다.
- **규칙**: 초기 진단은 `mudId`, `stageId`, 단계·시뮬레이터·선택·완료 시각, 정답 여부, 재시도 횟수 정도만 현재 세션 또는 localStorage에 기록한다. 이름·학번·계정·기기 식별자·장기 추적은 저장하지 않는다.

## D-014 — 유물은 수집 보상에서 역사적 추론 자원으로 확장한다

- **상태**: 채택 방향 (최소 실험 전)
- **근거**: 학생이 유물 획득 직후 “이 유물로 무엇을 하나요?”라고 질문했고, 그 지점에서 비교·경쟁·타이쿤 아이디어가 파생되었다.
- **규칙**: 첫 실험은 해금 유물 2개를 관찰·비교하고 역사적 주장과 근거를 연결하는 짧은 활동으로 제한한다. 성장·등급·경쟁은 점수나 공격력이 아니라 이해의 깊이와 근거 설명을 중심으로 설계한다.

## D-015 — 확장 기능은 서버 없는 작은 실험부터 검토한다

- **상태**: 기존 단계적 실험 원칙 (데이터 확장 방향은 D-017로 보완)
- **근거**: 현재 제품은 정적 호스팅·로그인 없음·localStorage라는 운영 경계를 갖고 있으며, 학생의 경쟁·타이쿤 요구는 아직 방향 가설이다.
- **규칙**: 유물 탐구, 친구 비교·협력, 역사 타이쿤은 NOW의 콘텐츠 QA와 진단을 거친 뒤 순서대로 작은 프로토타입을 검토한다. 백엔드·DB·실시간 멀티플레이·공개 순위표는 현재 구현하지 않는다.

## D-016 — Vercel을 목표 정적 배포 경로로 검토한다

- **상태**: 목표 방향 (현재 운영은 GitHub Pages)
- **근거**: GitHub push 기반 자동 배포와 향후 Supabase 연동을 고려하되, 현재의 HTML·CSS·Vanilla JS 구조와 정적 실행성은 유지한다.
- **규칙**: Vercel 설정 변경이나 실제 이전은 별도 검토·검증 후 진행한다. 이 결정만으로 현재 배포를 변경하지 않는다.

## D-017 — Supabase는 익명 데이터부터 점진적으로 도입한다

- **상태**: `보류` (2026-09-06, [D-020](#d-020--백엔드는-하나만-운영하고-supabase-계획은-d-019-결정에-따라-보류한다)로 대체). 프로젝트·테이블·코드 미구현 상태로 중단했다. 아래 개인정보·보안 원칙은 백엔드와 무관하게 계속 유효하다.
- **근거**: 빠른 완료 원인과 수업 효과를 확인할 최소 데이터가 필요하지만, 학생 계정이나 대규모 서버 기능은 현재 범위를 넘어선다.
- **규칙**: 첫 후보는 `play_events` 기반 익명 플레이 로그다. localStorage는 즉시 상태·복원·오프라인 대응을 유지하고, Supabase는 장기 익명 이벤트와 집계 후보로 한정한다.
- **보안**: 이름·학번·이메일·계정·기기 ID·IP 기반 식별·위치를 저장하지 않는다. 실제 도입 시 공개 클라이언트 키와 RLS를 사용하고 secret/service-role 키를 브라우저에 포함하지 않는다.
- **후속 게이트**: Anonymous Auth는 여러 활동의 관계 유지 필요가 확인될 때, Realtime은 학급 공동·협력 활동이 검증된 뒤 검토한다. 역사 타이쿤 장기 상태는 그 이후의 Future 항목이다.

## D-018 — Deep-dive는 여러 차시 연결을 우선 기준으로 하고 단일 세션 시간을 강제하지 않는다

- 상태: 채택 (2026-09-03)
- 근거: Deep-dive는 D-004에서 정의한 여러 차시 확장 탐구다. 4종의 구조적 시간 추정은 단일 세션의 30분 이상을 보장하지 않으며, 실제 수업 시간은 읽기·재시도·모둠 상호작용에 따라 달라진다.
- 규칙: `lessonNumbers`로 대단원 내 여러 차시 연결을 명시한다. 단일 세션 시간은 실측 전까지 추정 범위로 표시하고 실제 교실 관찰로 갱신한다. 특정 분을 채우기 위한 문제·분기 증가는 금지하며, 콘텐츠 확장은 학습 효과 검증을 근거로만 한다.

## D-019 — 실시간 협동 MUD의 백엔드는 Convex로 하고 별도 앱으로 분리한다

- 상태: 채택 방향 (2026-09-06, 프로젝트·코드 미구현)
- 근거: 2026-09-04 실제 수업(`EXPERIMENTS.md` `EXP-006`)에서 교사가 꼽은 최대 제약이 "모둠 진행 상황을 볼 수 없음"이었다. 교사 대시보드와 실시간 개입은 기기별 정적 구조로는 만들 수 없다. 기존 `js/mudEngine.js`는 전역 객체·고정 DOM·Canvas 기반 개인 플레이 엔진이라 네트워크 상태 저장소로 교체하기 어렵다.
- 규칙: 실시간 협동 MUD는 기존 GitHub Pages 앱을 개조하지 않고 별도 Next.js 앱으로 만든다. 공동 상태의 기준은 Convex이며, 화면 컴포넌트에 별도의 게임 상태 머신이나 WebSocket 서버를 만들지 않는다. 배포는 Vercel이며 기존 Pages 운영을 대체하지 않는다.
- 권한: 클라이언트가 보낸 `teacherId`·`playerId`·`roleId`를 신뢰하지 않고, 모든 공개 함수에서 인증 → 세션 소유·참가 → 모둠 → 허용 단계 순으로 검증한다. Convex에는 Supabase RLS에 해당하는 선언적 정책이 없으므로 권한 규칙을 직접 구현한다.
- 개인정보: 이름·학번·이메일·음성·화면 녹화·기기 고유 ID·위치를 수집하지 않는다. 수업 한정 가명 코드만 사용하고 보관 기간과 삭제 경로를 구현 전에 확정한다.
- 미검증: Convex Auth는 beta이고 Next.js 서버 인증 지원은 실험 단계다. 요금·무료 한도 안에서 수업 사용량을 감당하는지, 학교 Wi-Fi에서 21대 동시 접속이 되는지는 확인하지 않았다. 첫 수직 슬라이스에서 인증·복구를 먼저 검증하고, 실패하면 인증 계층만 재선정한다.
- 설계 문서: [`COLLABORATIVE_MUD_PLAN.md`](./docs/plans/COLLABORATIVE_MUD_PLAN.md), [`COLLABORATIVE_MUD_ARCHITECTURE.md`](./docs/plans/COLLABORATIVE_MUD_ARCHITECTURE.md), [`COLLABORATIVE_MUD_MVP.md`](./docs/plans/COLLABORATIVE_MUD_MVP.md)

## D-020 — 백엔드는 하나만 운영하고 Supabase 계획은 D-019 결정에 따라 보류한다

- 상태: 보류 결정 (2026-09-06)
- 근거: `D-017`은 기존 정적 앱의 익명 플레이 로그용으로 Supabase를 후보로 두었고, `D-019`는 신규 실시간 앱에 Convex를 채택했다. 두 계획을 모두 유지하면 1인 유지보수자가 BaaS 두 개를 동시에 운영하게 된다. 어느 쪽도 아직 구현되지 않아 지금이 정리 시점이다.
- 규칙: `D-017`의 Supabase `play_events`는 `보류`로 두고 새로 착수하지 않는다. 최소 플레이 진단 로그가 다시 필요해지면 Supabase 프로젝트를 새로 만들지 말고 Convex 위에서 재설계할지 먼저 검토한다. `TECH_STACK.md`·`ARCHITECTURE.md`·`ROADMAP.md`·`PRD.md`의 Supabase 서술은 "보류된 후보"로 읽는다.
- 예외: 이 결정은 백엔드 서비스 선택에만 적용된다. `D-017`의 개인정보 원칙(이름·학번·계정·기기 ID·위치 미수집, 클라이언트에 secret 키 미포함)은 백엔드와 무관하게 계속 유효하다.
- 재검토 조건: Convex의 요금·학교 네트워크·인증 검증이 실패해 백엔드를 바꿔야 할 때 이 결정을 다시 연다.

## D-021 — 학생의 판단 내용은 서버에 저장하지 않는다

- 상태: 채택 방향 (2026-09-06, 실시간 앱 미구현)
- 근거: 앱은 이름을 수집하지 않지만 교사가 `모둠·번호 = 실제 학생` 대응표를 갖고 있어, 서버의 개인 응답과 결합하면 특정 학생의 판단이 식별된다. Convex Cloud는 미국 동부·아일랜드 리전만 제공하므로 어느 쪽도 국내 보관이 아니다. 정적 버전에는 없던 위험이다. 한편 2026-09-04 수업([`EXP-006`](./EXPERIMENTS.md))에서 교사가 꼽은 제약은 "모둠 진행 상황을 볼 수 없음"이었지 "학생이 무엇을 골랐는지 볼 수 없음"이 아니었다. 대시보드에 필요한 것은 답이 아니라 진행 상태다.
- 규칙 1 — **개인 응답 비저장**: 최초·수정 판단의 선택지와 이유, 역사 비교 응답은 학생 기기의 세션 한정 메모리나 `sessionStorage`에만 둔다. 서버에는 제출 여부와 시각만 저장한다. 모둠 법 초안·확정과 법 확인 응답(`agree`/`discuss`)은 공동 산출물이므로 서버에 둔다. 기기의 개인 응답도 활동 종료 시 지우고 다음 협동 MUD로 넘기지 않는다.
- 규칙 2 — **보관 0일**: `수업 종료`가 곧 삭제다. 세션 하위 rooms/players/actions/events와 입장·복구 자격을 함께 지운다. 교사가 남기고 싶은 관찰은 앱 밖에 옮겨 적는다.
- 규칙 3 — **대응표를 앱에 넣지 않는다**: 학생 명단 업로드 기능을 만들지 않는다. 편의를 이유로 추가하면 규칙 1·2가 무의미해진다.
- 포기하는 것과 대체: 교사가 화면에서 개인 선택을 열어 볼 수 없다 → 그 자리로 가서 학생 화면을 본다. 판단 변경을 서버에서 집계할 수 없다 → 학생 기기 요약 화면과 모둠 대표 발표로 대체한다(2026-09-04 수업의 실제 운영 방식).
- 남는 조건: 제출 여부와 모둠 산출물은 수업 중 잠시 외부 서버에 존재한다. **개인정보 처리 근거와 Convex·Vercel 처리위탁·국외 처리 조건을 착수 전에 확인한다**(`BACKLOG.md` P1-COLLAB-PRIVACY). 국외 처리가 불가하면 [D-019](#d-019--실시간-협동-mud의-백엔드는-convex로-하고-별도-앱으로-분리한다)를 다시 연다. 이 결정의 설계는 백엔드와 무관하게 유효하다.
- 적용 문서: [`COLLABORATIVE_MUD_ARCHITECTURE.md`](./docs/plans/COLLABORATIVE_MUD_ARCHITECTURE.md) §2-3·§3 스키마·§8, [`COLLABORATIVE_MUD_MVP.md`](./docs/plans/COLLABORATIVE_MUD_MVP.md) §5·§6·§7·§11

## D-022 — 이 프로젝트는 교사 제작 교육용 저작물로 운영한다

- 상태: 채택 (2026-09-07, 2026-09-08 사용자 재확인)
- 결정: 이 프로젝트는 학교 교육과정 운영 지원을 목적으로 외부에서 개발·보급된 학습지원 소프트웨어가 아니라, 교사가 자기 수업을 위해 직접 구성·개발하는 교육용 저작물이다. 따라서 학습지원 소프트웨어 선정기준 확인과 학교운영위원회 심의를 프로젝트 착수 게이트로 두지 않는다.
- 판단 근거: 사용자가 2026-09-08에 “학교 교육과정 운영 지원을 목적으로 개발·보급된 것이 아니므로 심의 불필요”라고 범위를 확정했다. 이 결정은 제품의 실제 개발·보급 주체와 사용 목적이 달라지면 다시 검토한다.
- 범위: 이 결정은 교육자료 분류에만 적용한다. Convex·Vercel이 임시 호, 참여 ID, 모둠 구성, 진행 상태를 처리할 때의 개인정보 최소수집·처리위탁·국외 처리·파기·안전조치 검토는 계속 적용한다.
- 개인정보 원칙: 학생의 이름·학번·이메일·계정을 수집하지 않고, 활동별 임시 식별정보는 다음 활동과 연결하지 않으며 활동 종료 시 삭제한다. 개인의 선택과 이유는 서버에 저장하지 않는다.
- 사용자 제시 근거: [KDI 경제교육·정보센터 정책자료 첨부](https://eiec.kdi.re.kr/policy/callDownload.do?num=275455&filenum=2&dtime=20260516215955), [에듀집 안내](https://edzip.kr/main-notice/6a4de3afa450e64b79a52ce4), [한국교총 안내](https://www.kfta.or.kr/usr/wap/detail.do?app=16527&seq=270003468225). 실제 원문은 INBOX의 2026-09-07 항목에 보존한다.

## D-023 — 실시간 협동 MUD의 학생 식별은 활동마다 새로 만들고 종료 시 끊는다

- 상태: 채택 방향 (2026-09-07, 구현 전)
- 활동 시작: 학생은 협동 MUD마다 무작위로 제시된 공용 목록에서 새로운 호를 고른다. 시작 시점까지 고르지 않은 학생에게는 서버가 호를 무작위 배정한다. 대기 중인 학생은 서버가 해당 활동의 모둠과 역할에 무작위로 배치하며, 이전 활동의 호·참여 ID·모둠 정보를 재사용하지 않는다.
- 활동 중: 교사 대시보드는 실제 이름이나 개인 답 대신 호·모둠 구성·현재 단계·제출 인원·경과·도움 요청을 보여 준다. 교사는 진행 속도와 구성에 따라 미리 정한 힌트 또는 심화 상황을 모둠 단위로 보낸다.
- 활동 종료: 세션, 참여 ID, 호, 모둠·역할 배치, 진행 상태, 제출 상태·시각, 힌트·심화 이력, 입장·복구 자격을 삭제한다. 기기에 남은 세션 한정 응답도 지우며 다음 협동 MUD로 기록을 넘기지 않는다.
- 실패 대응: 자동 삭제 실패를 교사에게 알리고 수동 강제삭제를 제공한다. 공급자 로그·백업의 잔존기간은 [P1-COLLAB-PRIVACY](./BACKLOG.md)에서 별도 확인한다.
